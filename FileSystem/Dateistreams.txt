R4SYS file streams and atomic publication
==========================================

R4SYS file streams provide bounded, sequential transfer without requiring a
whole file in application memory.

- `begin` creates and owns a stream slot for one target.
- `write` accepts the next exact offset and advances the durable size.
- `finish` validates the expected final size and commits the stream.
- `abort` removes the owned partial file or reports a visible cleanup error.
- Conflicting file mutations invalidate the stream rather than continuing
  with stale directory or extent state.

Large streams may batch metadata and writeback, but `finish`, explicit flush,
atomic replacement and shutdown remain durability boundaries. Callers keep
their buffers valid for the complete operation.

Uploads that will be atomically published can declare a persistent publish
claim. Boot recovery completes or rolls back the claim according to the last
durable point. Cleanup is identity-checked: an unrelated file that later
appears under the same path is never deleted as if it were the old partial
upload.

Copying (0.81.27)
-----------------
SDK file_stream.copy and Files.copy use the optional file_copy_buffered slot
(R4SYS revision 18, Query:1 unchanged). The paired request retains both mount
uses, task unwind lifetime and canonically ordered volume mutation leases.
Backend gates cover lookup/stage creation and individual read/append/flush
calls. A completed chunk yields with those backend gates released, so other
tasks can read files or metadata on either volume. Other writers remain
excluded for the complete copy. This is not per-file writer parallelism.
Nested legacy callers retain exclusive gates if a parent still owns one.
Read-to-write upgrades never wait while holding a backend gate needed by an
existing mutation owner. Mount-use retention also excludes unmount/claims.

Native copy, buffered copy and the move fallback use the same transfer path.
Each chunk is at most 32 KB. Native callers own one private heap buffer;
buffered callers exclusively own their nonempty buffer until return. The
32-byte FileCopyProgress reports source size, completed staged bytes, append
count and maximum chunk. Errors, including cancellation, do not turn this
progress into a retry offset or durability claim. Older providers return
unavailable without a truncate/read fallback; the C facade has parity.

Source identity is checked before destination mutation: NTFS record/sequence,
FAT short-entry location or a shared nonempty chain. The inline source Entry
and mount lease survive individual calls; no backend scratch pointer escapes.
A bounded unique 8.3 stage is built in the target directory. The old target
remains readable until the final metadata phase. Source/target aliases,
read-only targets, allocation errors and cancellation before publication do
not replace it. Prepublication cleanup attempts to delete the owned stage;
cleanup I/O failure can retain it, but memory/leases always unwind.

After flushing the stage, the final backend gate deletes the old target and
publishes the completed stage. This retains the existing non-atomic copy
contract. FAT persists detachment of the short stage owner before installing
the new target (including LFN), preventing two durable names that each free
the same chain. A failure at this boundary can leave a missing/partial target
or orphaned completed payload; publication is never automatically retried,
rolled back or followed by unsafe stage deletion. Atomic document replacement
still uses replaceAtomic. NTFS uses its existing same-directory rename path.
A move deletes its source only after successful copy publication; a failed
source delete can leave both complete files, as before.

A2-0014 evidence: existing storage-test extended with the bounded transfer
case; actual request and R4SYS adapter bodies tested on the host for reader
admission, mutation exclusion, retained uses, nested admission rollback,
aliases, OOM, cancel, read/append/flush failure and caller buffers. A 70001-byte
copy uses 3 chunks (maximum 32768 bytes); an 8192-byte caller buffer uses 9.
The FAT publication-body test injects each detach/flush/publish boundary.
Kernel build and existing QEMU testquick with four vCPUs also passed.
These are software-work and ownership checks, not physical latency results.


NTFS random-write failure progress (0.78.29)
------------------------------------------
ntfs_volume.writeFileAtProgress reports status, completed bytes and an
uncertain flag. The byte count covers fully acknowledged device callbacks;
a failing boolean callback may have changed additional bytes. The existing
R4SYS random-write byte-count API preserves a positive short prefix. A final
metadata error after every payload byte is never converted to full success.
This count is neither a durability claim nor an automatic retry position.

Ordinary successful nonresident overwrites retain lazy payload writeback.
An ambiguous payload failure attempts a durable dirty marker and latches
repair_required in the mounted volume. Later normal writes and deferred
completion cannot clear that latch. If the device also rejects dirty-marker
I/O, cleanup_failed reports the unconfirmed state. Resident data lives inside
a FILE record: a durable dirty bracket protects the write, and an I/O failure
restores and flushes the original complete record before declaring rollback.
Failure of that restoration leaves the mount latched and the volume dirty
when its marker can still be stored. Atomic payload replacement or power-loss
recovery is not provided by this operation.

MFT growth retains its allocated runs until publication is confirmed or
rolled back. After possible record-0 publication, rollback must restore and
flush both record and mirror before releasing the newly allocated clusters.
A failed restoration retains potentially referenced clusters and returns
cleanup_failed. Allocation, zeroing and record I/O errors remain I/O errors;
no_space is reserved for actual capacity exhaustion. A lost record-bitmap
write completion also rolls its possible allocation back before clean state.

The repair latch belongs to one live mount; the on-disk dirty bit survives
remount. A general repair/admission policy for already-dirty volumes remains
outside this change. Such a remount is not proof that file contents have
been repaired, and ordinary write progress cannot serve as a repair result.
