R4OS 0.79.10: Firmwarevorbereitung und dateigebundene Modulressourcen
==================================================================

Stand: 11.09.2026. Software-Zwischenstand; 0.79.10 bleibt offen.
RELEASE_VERSION bleibt 0.79.9; Kernel 0.1.147, NVIDIA 0.1.21, DISPLAYD 0.1.9.
OssiPC: Release 0.79.8 und NVIDIA 0.1.21 nach normalem Neustart und exaktem
Modul-Rueckdownload bestaetigt. GSP-/VRAM-Vorpruefung und TCM-Grenzen sind
jetzt real gemessen. GPU-Ausfuehrung und native Anzeige bleiben offen.
Die nachfolgenden aelteren Abschnitte sind historische Zwischenstaende.

GSP-/VRAM-Vorpruefung (NVIDIA 0.1.21)
------------------------------------
Der externe Treiber liest maximal sechs gemessene BAR0-Seiten mit 13
benannten Registern. RISC-V und VGA werden nur nach lesbarer, aktivierter
Faehigkeit abgefragt. Zwei identische Durchgaenge muessen innerhalb einer
monotonen Sekunde enden. Fehlende, wechselnde oder geschuetzte Werte
(FFFFFFFF/BADFxxxx) erlauben keine weitere Initialisierung. MMIO-Fenster
schliessen vor DMA-Mapping, Pin und CPU-Puffer; fehlgeschlagener Cleanup
behaelt den Besitzer fuer einen spaeteren Shutdown-Versuch.

Aktuelle Hardwareaufnahme:

- IMEM 64 KB, DMEM 64 KB. Das unveraenderte FWSEC-Abbild mit
  57856 Code- und 2048 Datenbytes passt vollstaendig in beide Zielbereiche.
- Reset=false, RESET_READY-Hinweis=false, Scrubbing=false.
  Falcon halted=true, DMA idle=true, DMA full=false.
- RISC-V enabled=true, selected=false, active=false,
  halted=true, BCR valid=true. GSP-Besitz bleibt ungeklärt.
- Nutzbarer VRAM: 12288 MB aus dem Devinit-Register, nicht BAR1.
  WPR2 up=false, LO=0x1ffffe0000, HI=0x0 (Adressfelder).
- Display enabled=true, VGA valid=true, aktuelle Basis
  0x10e0000; Relocation benoetigt=true. Der aktuelle
  VGA-Bereich bleibt erhalten. Kein FRTS-/WPR-Bereich wird daraus allokiert.

NVIDIA 570.144 waehlt fuer GA106 den No-op-MMU-Lock-HAL; GA100-Lockregister
werden deshalb nicht gelesen. RESET_READY ist wegen eines dokumentierten
Hardwarefehlers nur ein Hinweis. Bei inaktivem WPR2 ist LO keine aktive
Reservierung. WPR-HI wird nicht als exklusives Bereichsende ausgegeben.
Registeraufnahme und passende TCM-Grenzen beweisen weder Reset, Quieszenz,
Firmwareauthentifizierung noch Wiederherstellbarkeit.

Gezielte Abnahme: bestehender NVIDIA-unit-test 31/31 Faelle, Modulbau 7/7.
Ein neuer Fall im bestehenden FWSEC-Test prueft Schutzwerte und reale
Kapazitaetsgrenzen; der vorhandene Lebenszyklusfall umfasst jetzt 38 Szenarien.
Eine anfangs falsche Zuordnung der gemeinsam genutzten Fuse-Seite im Hostfixture
wurde korrigiert; der folgende Lauf bestand. Kein neues Gate, Gesamt-/Langlauf,
erneuter RM-/Bootstrapbau oder zusaetzlicher Gastlauf.

OssiPC: Bootfb 800x600, Generation 1/Reset 0/Owner 0, 14 automatische
Dienste ohne Fehler; CONFIG.R4S bytegleich. Hisense laut User ausgeschaltet
und stromversorgt. Keine neue EDID-/HPD-/ELD-, Bild- oder Tonabnahme.
OSSIGFX 0.1.10: Batch/Transaktion 10, Journalgeneration 112.
Paket 92387590 Bytes, SHA256 7722236792750fca1a443ff48754ca166793ecea0dbb91e3c183ea257a5141bf.
NVIDIA.R4D 92386544 Bytes, 156 KB allokiert,
2348 Relocations; fuenf unveraenderte nicht allokierte Ressourcen.
Modul-SHA256: 160866500af285811f7287332ecd280c825469bbd8c712d4d06cb67324e91e0e
Referenzarchiv: ExFiles/Reference/GFX/Nvidia/0.79.10/gsp-preflight-20260911
31 komplette Originalreferenzen mit Lizenzen; 218 gehashte Dateien,
davon 122 Besitzerquellen. Maschinenlesbar: gsp_preflight_checkpoint.

Offen bleiben kontrollierter Falcon-Reset, abgesicherter Bootdisplay-Erhalt,
FRTS-/WPR-Besitz, FWSEC-FRTS und GSP-Boot/RPC/IRQ. SB ist das originale
Unload-Kommando, kein Ersatz fuer die GSP-Startkette. Voller RM-/NVKMS-Link,
native Anzeige und HDMI-Audio sind ebenfalls offen. Keine Unterversion oder
Hardwareabnahme wird durch diese Vorpruefung abgeschlossen.

Historische FWSEC-DMA-Vorbereitung (NVIDIA 0.1.20)
--------------------------------------
Das unveraenderte 59904-Byte-SB-Image wird mit 256-Byte-Ausrichtung im
residenten Treiberheap angelegt. Die vorhandene R4D-DMA-API pinnt diesen
Speicher und bildet ihn als einen physischen Abschnitt ab: coherent,
to-device, 49-Bit-Adressmaske und begrenzter Bounce-Fallback. Das Mapping
synchronisiert die fertigen Imagebytes. CPU-Zeiger gelten nie als DMA-Adressen.
Freigabe erfolgt als Unmap -> Unpin -> CPU-Free. Fehlgeschlagene Freigaben
behalten exakte Deskriptoren und Backing fuer den wiederholten Shutdown.

Ladeparameter stammen aus dem originalen kgspExecuteHsFalcon_GA102, der laut
570.144-HAL auch GA106 bedient. Geprueft werden vollstaendige 256-Byte-Bloecke,
49-Bit-DMA-Basen, 24-Bit-TCM-Ziele, 32-Bit-Quelloffsets, IMEM-VA-Abzug und
die PKC-Position. V3 besitzt keine DMEM-VA und setzt kein DMTAG.

OssiPC meldet DMA-Adresse 0x1476200, ohne Bounce-Puffer.
IMEM: 226 Bloecke, Kommando 0x614; DMEM: 8 Bloecke, Kommando 0x600;
PKC-Adresse 0x5a4; Bootvektor 0. Die Image-Pruefsumme ist unveraendert:
226935650584f518953f96426860de5743aa2508e30effa57a2d0ff3d185758b
Mapping, Pin, CPU-Puffer und PROM-/Fuse-Fenster wurden vollstaendig freigegeben.
Bootfb ist aktiv (800x600), Generation 1/Reset 0/Owner 0;
14 automatische Dienste laufen.
CONFIG.R4S ist bytegleich. Der Hisense bleibt laut User aus und stromversorgt;
dieser Lauf bestaetigt weder neue EDID/HPD/ELD noch sichtbares Bild oder Ton.

Gezielte Abnahme: vorhandener NVIDIA-unit-test, 30/30 Faelle und 12/12 Schritte,
darunter ein erweiterter Lebenszyklusfall mit 28 Fuse-/Vorbereitungs-/DMA-
Szenarien; Modulbau 7/7 Schritte und obige Hardwareaufnahme. Kein neues Gate,
kein Gesamt-/Langlauf, kein wiederholter RM-/Bootstrapbau und kein zusaetzlicher
Gastlauf. Die anfangs falsche Zig-Alignment-Schreibweise im Hostfixture wurde
korrigiert; der folgende Besitzerlauf bestand vollstaendig.

NVIDIA.R4D: 92377824 Bytes, 148 KB allokiert,
2231 Relocations; fuenf unveraenderte, nicht allokierte Ressourcen.
Modul-SHA-256: d7161756d16ba1ab6be7284ff249004c7176656fa2acc5030ccee8f0c9680bc3
OSSIGFX 0.1.9: 92378908 Bytes, Zielrelease 0.79.8.
Paket-SHA-256: e7eecb8000fc1a58d2deda68b5d59584ce9252eb4235af8bfa6bf4c45bee57b5
SFTP-Rueckdownload, VERIFY, STAGE, COMMIT und RESUME-BATCH bestanden:
Batch/Transaktion 9, Journalgeneration 101.

Referenzen: sieben vollstaendige Originaldateien inklusive COPYING, gegen
den unveraenderten 570.144-Quellkatalog geprueft. Ablage samt Quellen/Belegen:
ExFiles/Reference/GFX/Nvidia/0.79.10/fwsec-dma-20260910
189 gehashte Dateien, darunter 120 Besitzerquellen.
Maschinenlesbarer Stand: fwsec_dma_checkpoint; alte Checkpoints bleiben erhalten.

Vor nativer Ausfuehrung fehlen aktuelle TCM-Kapazitaet, kontrollierter
Falcon-Reset und gesicherte Quieszenz/Wiederherstellung. SB ist im Original
das Unload-Kommando zur Wiederherstellung von Pre-OS-Anwendungen. Der GSP-Start
braucht insbesondere einen besessenen FRTS-/WPR-Bereich, FWSEC-FRTS und die
GSP-Bootkette. Es wurde keine DMA-Adresse an die GPU uebergeben und keine
Firmware gestartet. Voller RM-/NVKMS-Link, native Anzeige und HDMI-Audio
bleiben ebenfalls offen; keine Unterversion oder Hardwareabnahme wird dadurch
als abgeschlossen markiert.

Historische Fuse-Auswahl und CPU-Kommandoaufnahme (NVIDIA 0.1.19)
------------------------------------------------------------
GA106/A1 erlaubt mit gemessenem BAR0 von 16 MB die lesenden 4-KB-Fenster
0x820000 und 0x824000. Register 0x82074c liefert 00000001,
Ucode-9-Register 0x8241e0 liefert 00000003. Beide Werte wurden
zweimal identisch innerhalb der monotonen Ein-Sekunden-Frist gelesen.
Der originale GA100-HAL gilt auch fuer GA106: Bit 0 bestimmt Debug/Produktion,
die Firmwareversion ist hoechstes gesetztes Registerbit plus eins. Der
vollstaendige Wert wird ausgewertet, nicht auf 16 Bits gekuerzt.

Damit waehlt OssiPC Produktions-App 0x85, Falcon-Eintrag 9, Fuse-Version 2
und die dritte 384-Byte-Signatur bei ROM-Offset 0x4153c. Signatur-SHA-256:
9cb8d7107985347bc1eb3acf6eaf161624eb19808628655bba5ef58727e8f6df
Nach Schliessen der MMIO-Fenster entsteht eine eigene residente CPU-Kopie:
59904 Bytes, init_cmd=0x19, 24 Bytes SB-Eingabe und gewaehlte Signatur.
SHA-256 des vorbereiteten Images:
226935650584f518953f96426860de5743aa2508e30effa57a2d0ff3d185758b
ROM, Firmwarecode und sonstige Imagebereiche bleiben unveraendert. Ein
erfolgreicher Cleanup gibt auch diese Kopie wieder frei. Es gibt keine
GPU-Adresse, DMA-Uebertragung, Firmwareausfuehrung oder Signaturauthentifizierung.

Zugelassen sind eindeutige V3-Eintraege mit Target 7, Flags 1, Engine-Maske
0x400 und Ucode 1..16. Fehlende Variante/Signatur, unpassende Formate, zu kleine
Puffer und Ueberlappungen werden vor dem ersten Zielschreibzugriff abgewiesen.
FRTS besitzt einen reinen 48-Byte-Encoder mit ausgerichtetem FB-Versatz und
vollstaendig begrenztem 1-MB-Bereich; seine reale GPU-Zuordnung bleibt offen.
Das Padding ist explizit null. Ausgabe-/Arbeitsadressen bleiben opak.

prepare-bootstrap extrahiert die originalen C-Typen mechanisch aus dem
vollstaendig gehashten 570.144-Quellbaum und behaelt deren volle MIT-Notiz.
Der echte Zig-Encoder stimmt bytegleich mit diesen kompilierten C-Strukturen
ueberein: Header 4, Eintrag 8, Mapper 64, SB 24, Region 20, FRTS 48 Bytes;
init_cmd liegt bei 44 und die FRTS-Region bei 24. Vierzehn Originalreferenzen
und die Ableitung begleiten das Paket. Alle 26 urspruenglichen Bootstrap-
Artefakte bleiben bytegleich, ebenso das komplette Paket bei Wiederholung.
Der gemeinsame PS7-Prozessstarter unterstuetzt nun auch leere Argumentlisten.

29/29 Besitzertests und 12/12 Schritte bestanden. Neun Lebenszyklusfaelle
enthalten 17 neue Fuse-/Vorbereitungsszenarien, darunter Map/Unmap/Collect,
falsche Fenster, instabile Werte, Clock/Deadline, Speicher- und Freigabefehler.
Fehlgeschlagene Freigaben behalten exakt ihren Besitzer bis zum Shutdown-
Erfolg. 7/7 Modulbauschritte und der vorhandene SMP4/KVM-Fallbacklauf
bestanden (20.22 s). Kein neues regulaeres Gast-Gate.

NVIDIA.R4D: 92364352 Bytes, 136 KB allokiert, 2147 Relocations.
SHA-256: 3c26fa27e5651f2eae68a12d65f7ff87980838d815abd7c6db88296b4b1ab57e
Die fuenf nicht allokierten Ressourcen samt vollstaendigen Lizenzen bleiben
bytegleich. 51 C-Adapter und 13 private Anbieter sind weiterhin im R4D
verlinkt, ohne offene Modulsymbole, TLS oder statische Initialisierer.

OSSIGFX 0.1.8 fuer Release 0.79.8: 92365389 Bytes,
SHA-256 5cbb62ddb29d2baa1975ffcd99e67e30f2beb93c6adc847ebec4b48ecf4deeac.
SFTP-Rueckdownload, VERIFY, STAGE, COMMIT und RESUME-BATCH bestanden.
Batch 8, Transaktion 8, Journalgeneration 90: installed.
Das installierte Modul stimmt nach Rueckdownload exakt mit dem Artefakt
ueberein; CONFIG.R4S bleibt bytegleich passiv. Bootfb: Generation 1/Reset 0,
ausgeglichene Buffer-/Queue-Ressourcen, 14 laufende automatische Dienste.
Eine neue visuelle Abnahme wurde nicht vorgenommen.

Referenzarchiv mit 255 gehashten Belegdateien und 118 Besitzerquellen:
ExFiles/Reference/GFX/Nvidia/0.79.10/fwsec-prepare-20260910
Der aktuelle maschinenlesbare Stand liegt in fwsec_prepare_checkpoint.
Die vorherige Ablage fwsec-bootstrap-20260910 bleibt unveraendert erhalten.

Nach dieser Abnahme meldet der User den Wechsel vom Blackview zurueck zum
Hisense 65U8QF. Der Fernseher bleibt ausgeschaltet, aber mit Strom versorgt.
Die Messungen bis einschliesslich NVIDIA 0.1.19 gehoeren zur Blackview-
Anordnung. Nach dem Anschlusswechsel liegen noch keine neue EDID-/HPD-/
Audio- oder sichtbare Bildabnahme vor. Weitere Arbeiten beruecksichtigen
diese Randbedingung; alte Boot-EDID ist kein Nachweis des neuen Empfaengers.

Offen bleiben GPU-Speicherbesitz, FWSEC-Ausfuehrung, WPR/FRTS, GSP-Boot und
RPC/IRQ/DMA-Lebensdauer sowie der volle RM-/NVKMS-Ziellink. Der letzte volle
Quellbau hat weiterhin 325 RM- und 46 NVKMS-Abhaengigkeiten. Native Anzeige
und HDMI-Audio sind noch nicht umgesetzt; Unterversion 0.79.10 bleibt offen.

Historischer FWSEC-/Bootstrap-Zwischenstand (NVIDIA 0.1.18)
--------------------------------------------------------
NVIDIA 0.1.18 verarbeitet die vorhandene unveraenderte PROM-CPU-Kopie. BIT-p
behaelt alle 32 Zeigerbits; der originale Falcon-Versatz ergibt sich aus
erster e0-Erweiterung 0x24600 minus Basis-ROM 0xfe00, also 0x14800. Der
Falcon-Katalog bei 0x8861b enthaelt 16 Eintraege; FWSEC liegt in Eintrag
8/9 mit App-ID 0x45/0x85 und Target 7. Die Bezeichnung Debug/Produktion
kommt aus dem Format. Keine der beiden Varianten wurde zur Ausfuehrung
ausgewaehlt und keine GPU-Fuse-Version wurde bisher gemessen.

Beide V3-Deskriptoren besitzen 1196 Bytes, einschliesslich drei Signaturen
zu 384 Bytes. Ihre Versionsmaske ist 7. Jedes 59904-Byte-Image enthaelt
57856 Bytes Code und 2048 Bytes DMEM. Der Deskriptor liegt bei 0x32364 bzw.
0x41210, das Image bei 0x32810 bzw. 0x416bc. Sechs getrennte SHA-256-Werte
fuer Deskriptor, Image und Signaturen stehen in der begleitenden JSON und
im originalen Hardwareprotokoll. Der gesamte PROM-Hash bleibt ueber alle
Diagnosestaende hinweg identisch.

Zwei reale Formatannahmen wurden korrigiert. 0.1.16 verlangte fuer das
Reserved-Wort irrtuemlich null; das Board liefert 0x9249 und NVIDIA wertet
dieses Wort nicht aus. 0.1.17 deutete den Firmware-Ausgabebereich bei
0x01000000 als CPU-Offset. 0.1.18 erfasst Ausgabe und Arbeitsbereich als
opake Firmwareadressen ohne CPU-Zugriff. Nur die tatsaechliche Eingabe muss
vollstaendig im geladenen DMEM liegen: 64 Bytes ab Offset 1984. Ein einziges
zusaetzliches Eingabebyte wird weiterhin abgewiesen. Die Commandmaske wird
roh protokolliert; daraus wird keine Kommandounterstuetzung erfunden.

Der Parser begrenzt die validierte PCI-ROM-Kette, Code/Daten, Interfaces,
Signaturplatz, doppelte Varianten und Ueberlappungen. Ein Fehler liefert
keinen Teilkatalog; die bereits valide passive Boarddiagnose bleibt nutzbar.
Genau dies wurde mit 0.1.16/17 auch auf OssiPC beobachtet. Alle Versionen
geben dieselben besitzgebundenen PROM-Ressourcen vollstaendig frei.

Der neue ausdrueckliche Besitzerstep prepare-bootstrap erstellt 26 originale
Referenzartefakte aus NVIDIA 570.144 / Commit
8ec351aeb96a93a4bb69ccc12a542bf8a8df2b6f. Die originale HAL-Zuordnung nennt
fuer GA106 GA102-GSP-RM-Boot/Booter-Load/Booter-Unload und den generischen
TU102-SEC2-Loader. Debug-/Produktionsimages, Deskriptoren, Signaturen und
Patchmetadaten bleiben bytegleich. Die Hostvorbereitung waehlt keine
GPU-Signatur aus und fuegt dem R4D keine Ressource hinzu.

Vor Compiler oder Originaldecoder wird ein privater Snapshot aller 3156
gepinnten Quellen, Header, Bau- und Lizenzdateien geprueft. Originale
C-Initialisierer und utilGz mit NVGZ_USER liefern die Bytes; eine unabhaengige
begrenzt lesende .NET-Deflate-Implementierung muss sie vollstaendig bestaetigen.
Der Originaldecoder wird ausschliesslich auf diesen gesperrten Originalen
verwendet. Alle urspruenglichen Namen, Quellen, Groessen, Hashes und ganzen
Lizenzhinweise begleiten das atomar publizierte Referenzpaket. Ein erneuter
Lauf ist bytegleich; falsche Quellen und abweichende vorhandene Ausgaben
werden abgewiesen, ohne vorhandene Pakete zu veraendern.

Abnahme: 26/26 Besitzerfaelle in 12/12 Schritten, 6588 vorhandene C-Format-
pruefungen, Modulbuild 7/7 und drei Inspectorfaelle fuer V2, V3 und IFR+V3.
Der kurze passive SMP4-Gast besteht in 20.21 s. OssiPC installiert Paket
OSSIGFX 0.1.7 ueber SYSUPD-Batch 7; R4U-Rueckdownload und anschliessender
Rueckdownload des installierten NVIDIA.R4D stimmen jeweils im SHA-256.
NVIDIA.R4D: 92351664 Bytes, allokiertes Image 126976 Bytes (124 KB),
2040 Relocations und dieselben fuenf nicht allokierten Paketressourcen.

Nach dem Neustart: bootfb 1024x768, Generation 1, Reset 0, Owner 0,
ausgeglichene Buffer-/Queue-Ressourcen und 14 laufende Dienste ohne Fehler.
CONFIG.R4S bleibt bytegleich im passiven Modus. Eine neue visuelle Abnahme
wurde nicht vorgenommen. Die beiden ueberholten Inbox-Transportpakete
0.1.5/0.1.6 wurden nach Sicherung ihrer lokalen Originale entfernt; aktuelle
Systemdateien, letztes Paket und SYSUPD-Backups bleiben vorhanden.

Referenzen und 566 gehashte Belegdateien:
ExFiles/Reference/GFX/Nvidia/0.79.10/fwsec-bootstrap-20260910
Enthalten sind die drei Quellenstaende 0.1.16/17/18, Originalreferenzen mit
Lizenztexten, alle 26 Bootstrap-Artefakte, Rohdiagnosen, Tests und Updatebelege.
Der maschinenlesbare aktuelle Stand steht in fwsec_bootstrap_checkpoint
der begleitenden GrafikFirmware07910.json; aeltere Checkpoints bleiben historisch.

Offen bleiben die Fuse-/Signaturauswahl, FWSEC-Ausfuehrung, WPR/FRTS,
GSP-Boot mit Mailbox/RPC/IRQ und sicherer DMA-Lebensdauer sowie der volle
RM-/NVKMS-Ziellink. Die bereits verlinkten 51 C-Adapter/13 privaten Anbieter
sind CPU-Grundlagen; der letzte vollstaendige Quellbau nennt weiterhin
325 RM- und 46 NVKMS-Abhaengigkeiten. Native Bildausgabe und HDMI-Audio
sind dadurch noch nicht implementiert. Unterversion 0.79.10 bleibt offen.

Offline-Arbeit trotz Hardwareabhaengigkeiten
------------------------------------------
Die damals fehlende Board-/VBIOS-Aufnahme blockierte den realen GPU-Bringup.
Sie blockierte aber nicht die gemeinsame Ressourcenbehandlung, die exakte
Paketbildung und die unabhaengige Pruefung bereits festgelegter Firmware.
Diese Arbeiten werden aus 0.79.10 vorgezogen. Der Softwarefortschritt ist
kein Ersatz fuer eine Hardwareabnahme und schliesst keine Folgeversion ab.

Ein Quellen- und Versionsvertrag
-------------------------------
NVIDIA/src/firmware-lock.json ist die einzige Paketwahrheit: RM/GSP 570.144,
Upstream-Commit, offizielle Installerquelle, Installerhash, beide GSP-Dateien
mit Groesse und SHA-256, vollstaendige Lizenz sowie die acht originalen
Containerfamilien. Der Zig-Code liest die feste JSON beim Kompilieren; PS7
liest dieselbe Datei bei der Paketvorbereitung. Kein zweiter Firmwarehashkatalog.
NVIDIA.R4D traegt die Sperrdatei als NVFW-LOCK.json, beide Originalcontainer
und die vollstaendige Lizenz in seiner nicht allokierten .rsrc-Section.

Die bereits in 0.79.3 archivierten Originale bleiben unveraendert:
gsp_ga10x.bin mit 63571696 Bytes, gsp_tu10x.bin mit 28542040 Bytes und LICENSE
mit 17863 Bytes. Die Rohquellen liegen in
ExFiles/Reference/GFX/Nvidia/Firmware-570.144. Herkunft und OS-Portierungs-
entscheidungen: GrafikArchitektur07903.txt/.json sowie NVIDIA/PROVENANCE.txt.
Die Zuordnung zu acht NVIDIA-Containerfamilien bedeutet nicht, dass R4OS
diese acht GPU-Familien bereits unterstuetzt.

Paketbildung
------------
Der Besitzerstep prepare-firmware verwendet auf Windows und Linux denselben
PS7-Ablauf. SourceDirectory und ScratchDirectory werden ausdruecklich
angegeben. Ausgabe ist standardmaessig das ignorierte Firmware/ im NVIDIA-
Besitzer; ein optionales OutputDirectory erzeugt ein separates Paket, ohne
die Modulmanifestpfade zu veraendern. Scratch liegt unter Temp und fuer atomare
Publikation auf demselben Dateisystem wie Output. Der Treiber und diese
Hostaktion laden nichts herunter und fuehren keinen Installer aus.

Groesse und Hash aller drei Quellen werden vor dem Kopieren und erneut an
den privaten Kopien geprueft. Beide Container gehen durch den echten
Firmwareinspektor. Alle erwarteten Familiensignatursektionen werden dabei
gefunden und begrenzt. Die vollstaendige originale NVIDIA-Lizenz begleitet
die beiden unveraenderten Binaerdateien; sie werden nicht Apache-lizenziert.
Erst das vollstaendige Verzeichnis wird publiziert. Bei erneutem Aufruf
muessen Dateien, Sperrdatei und Befunde bytegenau mit dem vorhandenen Paket
uebereinstimmen. Abweichungen werden nicht still repariert.

module.R4MF ist die einzige Build-/Ressourcenwahrheit. Der normale Build
verlangt die vorbereiteten Originale und prueft Laenge/Hash vor Paketierung
und Installation. Fehlende oder beschaedigte Daten liefern einen Fehler und
lassen ein zuvor installiertes NVIDIA.R4D bytegleich. SDK-Pfade werden erst
im ausgewaehlten Paketstep geoeffnet; Vorbereitung und Hosttests bleiben
dadurch auch ohne die proprietaeren Quelldateien ausfuehrbar.
Die Binaerdateien sind weiterhin nicht im oeffentlichen Quellrepository.
Das erzeugte NVIDIA.R4D enthaelt sie und die komplette Originallizenz.
Distribution fuehrt denselben bytegleichen Lizenztext zusaetzlich unter
/R4OS/LICENSES/NVIDIA-570.144-LICENSE.txt und neben Images in Legal mit.
IMAGE_SCOPE=none bleibt erhalten; normale Images aktivieren NVIDIA nicht.

Begrenzter Lade- und Pruefpfad
-----------------------------
Der externe NVIDIA-Besitzer besitzt firmware.zig. Load schreibt pro Schritt
hoechstens 64 KB direkt in den vom Aufrufer bereitgestellten Endpuffer.
Es gibt keinen zweiten vollstaendigen Firmwarepuffer. Die gesamte Laenge und
der SHA-256 muessen exakt passen; erst danach liefern gueltige ELF-Struktur,
Versions- und Signatursektion eine nutzbare geliehene Sicht. Der Aufrufer
besitzt Allokation, Freigabe und Lebensdauer dieses unveraenderten Puffers.
Fehler und idempotentes Close geben keine neue gueltige Sicht frei.

ELF64 Little Endian ET_REL/EM_RISCV, maximal 64 MB, 64 Sections, 4096 Bytes
Namensbereich und 63 Bytes je Namen. Unterstuetzt wird das Containerprofil
der gesperrten Version: keine Programheader, Kompression oder ausfuehrbare
Section. Laengen, Ueberlaeufe, Ausrichtung, Namen, Mehrdeutigkeiten und
Bereichsueberlappungen werden geprueft; .fwimage, exakte .fwversion und die
passende 4096-Byte-Signatursektion sind erforderlich. Signaturbytes und
Firmwarecode bleiben opak. Bytegleichheit zum Hash ist keine GPU-seitige
kryptografische Authentifizierung und kein Beweis der Boardkompatibilitaet.

Monotone Zeit und absolute Deadline werden vor/nach dem Reader geprueft;
Clock-Ruecksprung, Ablauf, kurze Reads und I/O-Fehler sperren die Freigabe.
Der R4D-Adapter verwendet jetzt echte begrenzte Ressourcenreads. Die
Hostpruefung kann eine blockierende Betriebssystem-Dateioperation nicht
gewaltsam unterbrechen und weist ihre Deadlinegrenze entsprechend aus.
Keine GPU-DMA- oder IRQ-Nutzung entsteht durch diese Softwarepruefung.

Korrektur im gemeinsamen Modullader
----------------------------------
Der R4X-Lader trennte .rsrc bereits vom Programmimage; der gemeinsame
R4D/R4P/R4L-Lader allokierte und kopierte noch alle Sections. Kernel 0.1.138
wendet den vorhandenen R4M0-Vertrag nun auch dort an. Eine einzige .rsrc mit
Flags null, Alignment 16 und gleich grossen nichtleeren Datei-/Speicher-
Extentfeldern bleibt ausserhalb von Imageallokation und Sectionkopie.
R4L-Ressourcen, Ressourceneinspruenge, Exporte und beide Relocationenden
werden abgelehnt. Dieselbe Regel gilt fuer Datei- und Preloadbytes.
Die veroeffentlichte Laufzeitbasis der Dateisection ist null.

Die fachliche NVIDIA-Paketpruefung bleibt im externen Treiberprojekt.
Kernel 0.1.139 erfasst nun vor R4D-Publikation den begrenzten Ressourcen-
katalog und bewahrt die originale FileSource mit Mount-/Dateiknoten-Snapshot.
DriverApi29 bindet stat/read_at an Modul und echten Treiberstart. Alte und
fremde Handles werden abgelehnt; Beginn von Shutdown schliesst Admissions.
Reads halten den Lifecycle-Owner und den Dateisystem-Request bis zum sicheren
I/O-Ende. Worker treten ohne Lock-Wartezyklus ein; IRQ-/Storagecallbacks sind
ausgeschlossen. Es gibt weder frei waehlbare Pfade noch dauerhafte Leases.
Dateiinhalt ist nicht als unveraenderlich garantiert; Hashpins bleiben Pflicht.
Byte-/Preloadmodule besitzen keine gespeicherte Ressourcendatei und melden
source-unavailable. Die vorhandenen R4SYS-Appfunktionen bleiben unveraendert.

Die optionale Query steht nach dem unveraenderten 592-Byte-Praefix;
DriverApi29 ist 600 Bytes gross. Neue Payloads und Werte sind zentral
generiert, Zig-/C-Fassaden erhalten alte und kurze Tabellen. Genauer Vertrag:
Repositories/Contract/ABI/R4DDriver.txt, Abschnitt Loaded R4D resources.
now_ns ist eine reine monotone Uhr; Ressourcenoperationen sind ownergebunden.
64-KB-Bereichsreads pruefen Deadline und Uhr vor/nach synchronem Storage.
Kein erzwungener Hardwareabbruch: Cleanup darf die Deadline ueberschreiten.

NVIDIA liest seine 1241-Byte-Lockressource vor PCI ueber diesen echten
Pfad. Falsche/fehlende/kurze/abgelaufene Daten verhindern Init ohne PCI-Zugriff.
firmware_resources.Reader bindet den vorhandenen Load an exakten Ressourcen-
namen, Umfang und dieselbe Modulgeneration. NVIDIA 0.1.3 besitzt alle vier
Ressourcen und firmware_storage.Storage verwaltet den CPU-Endspeicher.
DriverApi28 erlaubt weiterhin nur die passive Basisprobe.

Laufzeitspeicher und Abschluss
-----------------------------
OPTION NVIDIA mode=firmware-check laedt beide Container nacheinander vor
dem passiven PCI-Pfad. Der Standard bleibt passive und liest keine GSP-
Nutzdaten. Ein normales GfxBuffer-Systemobjekt mit Write-Back-CPU-Map stellt
den Endpuffer; maximal 64 KB je Read und 30 Sekunden absolutes Budget je
Container einschliesslich Lockread und Allokation. Nur vollstaendige Hash-/
ELF-/Versionspruefung erzeugt eine geliehene ready-Sicht. Der positive
SMP4-Gast liest die Originale aus dem geladenen Modul mit 971/436 Reads.
GPU-Signaturannahme bleibt ausdruecklich unverified.

Close entwertet alle Sichten vor Unmap, Release und Collect. Die gecachte
Speichertabelle bleibt im echten Shutdownkontext verwendbar, obwohl neue
Query-/Allokationsadmission geschlossen ist. Fehlgeschlagener Rueckbau
behaelt Handles oder die ausstehende Collection; erfolgreicher mehrfacher
Abschluss bleibt idempotent. Jeweils nur ein Containerpuffer lebt; beide
werden geschlossen, bevor die normale passive PCI-Probe beginnt.

Kernel 0.1.140 korrigiert den gemeinsamen CPU-BO-Abschluss. Releasing- oder
Destroying-Objekte bleiben am exakten Produzenten samt Treiberstartepoche
sichtbar, selbst wenn kein oeffentlicher Referenzhandle mehr existiert.
Collect liefert bei ausstehendem VM-/TLB-Abschluss BUSY; die Loaderpruefung
verhindert Wiederverwendung der Owner-ID. Auch private Allokationsreste
werden so behalten. CPU-Seiten werden erst nach realem TLB-Abschluss frei.
Dieser Laufzeitpfad verlangt Kernel 0.1.140 oder neuer. Kein Firmwarepuffer
wird durch diesen Check als GPU-DMA gemappt und kein GPU-Register geschrieben.

Pruefung und offene Punkte
-------------------------
Dreizehn NVIDIA-Besitzerfaelle im vorhandenen unit-test, darunter zwei
Firmwarefaelle: alle 4672 Trunkierungen und 3072 Metadatenbitmutationen eines
synthetischen ELF sowie inkrementelle Hash-/Read-/Deadline-/Clock-/Close-
Fehler. Beide echte GSP-Container wurden mit 971 beziehungsweise 436 Reads
bis maximal 64 KB verifiziert. Falscher Hash, falsche Containerfamilie und
fehlende Paketquellen erzeugen keinen neuen Erfolgsbericht oder Paketpfad.
Ein zweiter Vorbereitungsdurchlauf bestaetigt dasselbe Paket unveraendert.

Der gemeinsame Kernel-Testlauf umfasst 410 Hostfaelle. Die bestehende
Modulladerprobe erweitert sich um zwei Ressourcenfaelle; eine virtuelle
64-MB-Ressource vergroessert das Image nicht. Eine alte API-Negativfixture
erwartete irrtuemlich weiterhin verpflichtende Providerfelder. Sie prueft
jetzt unbekannte Felder; fehlende optionale Callbacks sind vertragsgemaess.
Neue Dauer-Gates oder Emulator-Langlaeufe wurden nicht angelegt.
Der vorhandene Grafiklauf prueft nvidia-firmware mit beiden Originalen und
nvidia-firmware-missing/-corrupt mit privaten fehlerhaften Modulkopien im
SMP4-Gast. Alle verlangen echten Fehllade-Shutdown und nutzbaren bootfb ohne
NVIDIA. Fehlender Ressourcenname und falscher Hash sperren weitere PCI-Arbeit.
Ein ausgewaehlter Storage-Hostfall prueft Create/Map/Unmap/Release/Collect,
I/O/Kurzreads/Deadline, keine Teilfreigabe und die gecachte Shutdowntabelle.
Der Kernel-Besitzerfall prueft private CPU-Reste ohne Referenz, fremde Epoche,
beibehaltenes Budget und echten Abschluss nach aufgehobenem Backendfehler.
Konkrete Resultate, Quell-/Artefakt-/Imagehashes und Rohbelege:
GrafikFirmware07910.json und ExFiles/Reference/GFX/Nvidia/0.79.10.

Offen bleiben die Einbindung des originalen RM/NVKMS, FWSEC aus dem gemessenen Board-VBIOS,
GSP-Bootstrap/WPR, Mailboxes/RPC/Notifications, Generation und IRQ/DMA-
Rueckbau, Hardware-Deadlines, Thermik sowie reale Displaywiederherstellung.
Die naechste unabhaengige Softwarearbeit ist der R4OS-Adapter fuer den
ausgewaehlten originalen RM-/NVKMS-Pfad. Der Paketladepunkt von 0.79.10 ist
softwareseitig nachgewiesen; die Unterversion bleibt wegen des Bringups offen.
Hardware-Freigaben, HDMI-Audio, native Anzeige und Performance sind weiterhin
gesondert auf OssiPC beziehungsweise ausdruecklich vereinbarter Hardware
nachzuweisen. Ein Windows-Hostlauf wurde hier nicht ausgefuehrt.

Vorheriger Ressourcen-Zwischenstand: bestehende Testabdeckung
----------------------------------------------------------
Der Kernel-Ladertest prueft kanonische Kataloge ohne Payloadkopie, 4096
Trunkierungen, ungueltige Namen/Reihenfolgen/Padding und I/O-Ausfall sowie
fremde/veraltete/geschlossene Handles, Neustart und Generationsexhaustion.
NVIDIAs zweiter Lifecycle-Fall benutzt die echte SDK-/Treiberimplementierung
fuer Lock- und Ressourcenfehler. Resource-Fassade: sechs Zig-Faelle plus
C-Ausfuehrung, Grafik-Fassade: acht Zig-Faelle plus C-Ausfuehrung. Die
Contractpruefung bestaetigt 26 Negativmutationen, sechs Gruppen, 375 Slots
und 200 Payloads. Keine neue Gatefamilie und kein Emulator-Langlauf.

Der vorherige nvidia-passive-SMP4-Lauf unter KVM benoetigte 20,21 Sekunden.
Kernel aktiv/installiert 0.1.139; echter Lockread meldet Modulgeneration 1.
R4D-Image weiterhin 24576 Bytes; die 1289-Byte-Ressourcensection bleibt auf
dem Datentraeger. DISPLAYD, fehlender NVIDIA-Adapter, Fehllade-Shutdown und
bootfb sind erfolgreich. Kein OssiPC-Kontakt, keine GPU-/HDMI-Abnahme.
Belege dieses vorherigen Stands: GrafikFirmware07910.json -> resource_checkpoint und
ExFiles/Reference/GFX/Nvidia/0.79.10/resources. Die aelteren JSON-Felder und
das Archiv 0.79.10 beschreiben weiterhin den ersten .138/.1.1-Zwischenstand.

Aktueller Laufzeit-Zwischenstand
------------------------------
GrafikFirmware07910.json -> runtime_checkpoint und das getrennte Archiv
ExFiles/Reference/GFX/Nvidia/0.79.10/runtime enthalten die aktuellen Quell-,
Artefakt- und Imagehashes, Rohlogs, Paketfehler und die drei SMP4-Ergebnisse.
Das geladene NVIDIA-Image ist 45056 Bytes (44 KB) gross; die vier Ressourcen
bleiben ausserhalb dieser Allokation. Der positive Imagepfad enthaelt die
vollstaendige bytegleiche Lizenz auch als gemeinsame Beigabe. Die beiden
Negativgaeste verwenden denselben Kernel und Treibercode, vor Ergaenzung
dieser zusaetzlichen Image-Beigabe; ihre Modulressource enthielt die Lizenz.
Keine neue Gatefamilie, keine Realhardware und kein Emulator-Langlauf.

Erster RM-/NVKMS-Quellbau und Abhaengigkeitsnachweis ohne Shader
-------------------------------------------------------------
Der ausdrueckliche NVIDIA-Besitzerstep build-rm kompiliert die vollstaendigen
Quelllisten des gewaehlten 570.144-Stands: 971 RM- und 225 NVKMS-Dateien.
Quellbaum und Scratch werden absolut angegeben; Scratch liegt unter Temp.
Die PS7-Orchestrierung benutzt den Compiler des SDK-Buildgraphen und legt
pro Versuch einen privaten Quell-/Ausgabebaum an. Kein RM-Objekt wird in den
Treiber installiert; NVIDIA 0.1.3, Kernel 0.1.140 und Release 0.79.8 bleiben.

Ein separater Quellbeleg Tools/Rm/Sources.json bindet 3156 Quellen, Header,
Build- und Lizenzdateien mit zusammen 96017025 Bytes an die source_commit
der bestehenden firmware-lock.json. Er wurde aus den bereits archivierten,
einzeln gehashten Originalen hergeleitet, nachdem auch das Originalarchiv
erneut geprueft wurde. Es gibt keine zweite Firmwareversion oder neue Quelle.
Der private Snapshot wird vor Compilerstart anhand von Pfad, Laenge und SHA
geprueft. Fehlender Header, gleich langer veraenderter Header und zusaetzlicher
Header scheitern vor dem Quellbau; das installierte NVIDIA.R4D bleibt bytegleich.

Die Komponenten behalten ihre eigene Include-Reihenfolge und Defines samt
dateispezifischen SPDM-Optionen. Eine temporaere, lizenztreue Headerkopie
erweitert nur die Plattformabfrage fuer den Versionstext um NV_R4OS. PIC und
Freestanding fuer R4OS ersetzen Linux-Kernel-Code-Modell und Linux-OS-Makros.
Explizites -g0 verhindert implizite Debugdaten. Prefix-Mapping entfernt
Versuchspfade aus Code/Daten. Alle Herstellerquellen bleiben unveraendert.
Die ursprüngliche Sieben-Dateien-Probe aus 0.79.3 bleibt als historischer
Beleg erhalten; dieser Quellbau deckt jetzt beide vollstaendigen Listen ab.

Beide partiellen Links verwenden eindeutige deterministische ID-Texte; RM
behaelt Original-Exportwurzeln und Linkerscript. Automatische Initialisierer
und TLS: in beiden Objekten null. Noch offene Symbole:

Komponente  Gruppe                                 Anzahl
RM          allgemeine os_-Funktionen                 138
RM          nv_-Plattformanbindung                     92
RM          libspdm-Kryptobackend                       60
RM          NVSwitch-OS-Anbindung                       35
RM          NVLink-OS-Anbindung                         13
RM          nvidia_caps_root und out_string              2
NVKMS       nvkms_-OS-Anbindung                         52
NVKMS       acht Shaderdateien, je Start-/Endsymbol      16
NVKMS       memcpy und memset                            2

Diese Einteilung ist eine Abhaengigkeitsaufnahme, keine pauschale Zusage,
jede Linux-Funktion nachzubauen. Die Crypto-Implementierungen gehoeren beim
Original-Linux-Build zu kernel-open/nvidia/libspdm_*.c und binden die Linux
Crypto API ein; sie fehlen nicht aufgrund eines vergessenen RM-Includepfads.
R4OS braucht ein echtes Backend oder muss die betroffenen Eigenschaften
ausdruecklich ablehnen. NVIDIAs libspdm_check_crypto_backend prueft genau
diese Verfuegbarkeit. Erfolgsstubs ersetzen kein Backend und keine Signatur-
annahme. GPU-GSP-Authentifizierung und SPDM sind getrennte Pfade.

Die gemeinsamen globalen Definitionen enthalten nvstatusToString in beiden
Objekten. Der finale Link braucht eine eindeutige Zuordnung; auch die
originale RM-Lokalisierung von memcpy/memset steht noch aus. Die komprimierten
Shader waren in diesem ersten Quellbaunachweis noch nicht eingebettet; der
nachfolgende Abschnitt dokumentiert ihre Integration. GOTPCREL/GOTPCRELX in partiellen Objekten
sind keine vom R4M0-Lader freigegebenen Laufzeitrelokationen. Die ELF-Aufnahme
weist nur Relokationen allokierter Sections als ladebezogene Zwischenwerte
aus; ein finaler R4D-Link und dessen echte Relokationspruefung bleiben offen.

Belege dieses Softwarestands: GrafikFirmware07910.json -> rm_build_checkpoint
und ExFiles/Reference/GFX/Nvidia/0.79.10/rm-build. Sie enthalten Quellpin,
Compiler-/Linkplaene, Hashes, Fehlerprotokolle und die Symbol-/ELF-Inventare.
Zwei volle Besitzerlaeufe in getrennten Verzeichnissen, eines mit Leerzeichen,
benoetigten 140,97 und 139,84 Sekunden. Alle 1196 Objektdateien und beide
Teilobjekte sind jeweils bytegleich. Die offene Symbolliste stimmt mit einer
unabhaengigen GNU-nm-Auswertung ueberein. Bestehender NVIDIA-unit-test:
13/13 Faelle und 9/9 Schritte bestanden. Das installierte R4D ist bytegleich;
fuer diesen reinen Hostbau war deshalb kein erneuter Gastlauf erforderlich.
Originales COPYING, SoftFloat-Lizenz und Dateihinweise bleiben im privaten
Quellbaum; vor einer Redistribution der RM-Objekte sind sie beizulegen.
Windows wurde nicht ausgefuehrt. Kein GPU-/HDMI-/Performance- oder Hardware-
nachweis, keine zusaetzliche Gatefamilie. Die R4OS-OS-Anbindung bleibt der
naechste unabhaengige Implementierungspunkt; 0.79.10 bleibt offen.

NVKMS-Shader im regulaeren Quellbau
---------------------------------
Der bestehende build-rm-Step baut und prueft jetzt auch die acht Pakete aus
der originalen COMPRESS_SHADERS-Liste. XZ Utils gehoert unter Debian bereits
zu den DevKit-Voraussetzungen; auf beiden Hosts kann ein absoluter -XzPath
angegeben werden, etwa fuer ein Windows-xz.exe. Sonst wird xz aus PATH
aufgeloest. Der Treiberbesitzer laedt kein Hostwerkzeug herunter.

Die Originaldateien und Metadaten sind schon im Quellkatalog enthalten.
Ihre ProgramHeapSize muss der jeweiligen Originaldateigroesse entsprechen.
XZ --extreme --check=none folgt NVIDIAs Makefile, --threads=1 legt den
Einthread-Modus fest; XZ_OPT und XZ_DEFAULTS werden entfernt. Pipe-/Datei-
zugriffe sind binaer. Native.ps1 teilt Prozessstart, Fristen und Diagnose
zwischen Shaderbau und Link; auch blockiertes stdin wird vom Fristabbruch
erfasst. Ein vorzeitig fehlgeschlagener Prozess behaelt Exitcode und stderr.

Der Hostpruefer kompiliert die drei originalen XZ-Decoderquellen mit den
NVKMS-Defines. Die originalen nv3dImport-Speicherhooks werden ueber echte
Hostallokation und memcpy/memmove/memset/memcmp erfuellt. XZ_SINGLE muss
vollstaendigen Input, die exakte Ausgabe und anschliessend Bytegleichheit
zum Original liefern. Das ist keine R4OS- oder GPU-Laufzeitimplementierung.

Shader      Originalbytes    XZ-Bytes (XZ Utils 5.8.1)
Maxwell            171776       23960
Pascal             171776       23960
Volta              250240       18960
Turing             247296       16688
Ampere             244480       17532
Hopper             244224       16964
Blackwell          253184       16512
GB20x              252288       19100
Gesamt            1835264      153676

Die acht Objektdateien enthalten allokierbare, nicht schreibbare und nicht
ausfuehrbare .rodata mit 16-Byte-Ausrichtung. Eine unabhaengige ELF-Auswertung
prueft alle sechzehn Symbole, exakte Start-/Endspannen und echte Nutzdaten-
hashes sowohl in den Einzelobjekten als auch im NVKMS-Teilobjekt. Sie sind
zwischen getrennten Versuchen einschliesslich Leerzeichenpfaden bytegleich.
NVKMS hat danach 54 offene Symbole: 52 OS-Funktionen sowie memcpy/memset.
RM behaelt seine 340 offenen Symbole und denselben Objektinhalt. TLS und
automatische Initialisierer bleiben in beiden Komponenten null.

Der vollstaendige Besitzerlauf besteht in 146,47 Sekunden. Der echte Decoder
weist einen beschaedigten Header-CRC, abgeschnittenen Footer, Anhang und
abweichende dekodierte Daten ab. Ein blockierter Kompressor wird beim
Einsekundenbudget nach 1,135 Sekunden beendet; frueher Exit 23 behaelt seine
Diagnose. Der Hostpruefer ist auch fuer x86_64-windows-gnu uebersetzt; auf
Windows ausgefuehrt wurde er nicht. Bestehender NVIDIA-unit-test: 13/13
Faelle, 9/9 Schritte. Das installierte NVIDIA.R4D bleibt bytegleich; dieser
Hostschritt braucht keinen weiteren Gastlauf und keine neue Gatefamilie.

Ein erster voller Versuch scheiterte nur an der Summenbildung fuer die
PowerShell-OrderedDictionary-Daten. Er bleibt als Fehlversuch erhalten.
Der anschliessende Komponentenlauf und der frische volle Besitzerlauf
verwenden die korrigierte Summenbildung und bestehen. Keine Originalquelle
wurde dafuer veraendert. Belege: GrafikFirmware07910.json -> shader_checkpoint
und ExFiles/Reference/GFX/Nvidia/0.79.10/shaders.

Die Shader und ihre Objektdateien bleiben im privaten Quellbau mit den
vollstaendigen Originalquellen und Lizenzen. Sie sind nicht in NVIDIA.R4D
installiert. Echte OS-Adapter, eindeutige globale Symbole, finale R4M0-
Relokationen, GPU-Ausfuehrung und Hardwareabnahmen bleiben offen. Kernel,
Treibermodul und Release behalten ihre Versionen; kein OssiPC-Kontakt.

CPU-Speicheradapter gegen die Originalschnittstellen
----------------------------------------------------
Der Quellbau enthaelt jetzt acht RM- und neun NVKMS-Adapter fuer Allokation,
Freigabe, Kopie/Fuellen, Vergleich und Zeichenketten; NVKMS zudem memmove.
Die zwei C-Einheiten binden unveraenderte Herstellerheader ein. Integer-
register und exakte REP-Wort-/Bytetails verhindern eine Rekursion durch
RM gcc_helper.c (memcpy -> os_mem_copy). Keine SIMD-Abhaengigkeit, keine
Aenderung des Direction Flags. Ueberlappende Zielbereiche werden bei Bedarf
rueckwaerts kopiert. Vergleich ist unsigned; strncpy behaelt Nullauffuellung
und den auf n begrenzten, nicht zwingend abgeschlossenen Eingabetext.
Alle Pfade betreffen CPU-RAM; MMIO und DMA haben weiterhin eigene Grenzen.

os_alloc_mem setzt seinen Ausgabepointer auch bei Fehler auf NULL, lehnt
einen fehlenden Ausgabeparameter ab und reicht die ganze NvU64-Laenge weiter.
Null Bytes werden auf ein Byte normalisiert. NVKMS zero fuellt ausschliesslich
die angeforderte Nutzlast. Der Anbieter besitzt die echte Allokationsgroesse;
Release rekonstruiert keine Speicherklasse aus dem NVKMS-Groessenargument.

In den getrennten RM-/NVKMS-Teilobjekten bleiben die zwei privaten Imports
r4nv_heap_allocate/free offen. Im hier dokumentierten Quellbau gab es noch
keinen R4OS-Anbieter. Der nachfolgende Laufzeitcheckpoint implementiert ihn
in NVIDIA.R4D; der finale kombinierte Link bleibt ausstehend. Der Host-
Testallocator des Quellbaus ist weiterhin ausschliesslich ein Hostnachweis.

Die aktuelle Kernelanbindung erlaubt GfxDriverMemoryApi-Operationen nur
innerhalb des globalen R4D-Lade-/Entladeguards. Hintergrund-Worker besitzen
zwar einen authentisierten Treiber, treten diesem Guard aber nicht automatisch
bei. Beim Ressourcen-I/O ist der Beitritt bewusst nicht blockierend, weil ein
anderer Treiber auf denselben Worker warten kann. Diese bestehende Grenze
darf ein allgemeiner RM-Allocator nicht durch eine spin-wartende Schleife oder
einen vorgetaeuschten Owner umgehen. Der R4X-SDK-VM-Allocator gehoert wiederum
einer Programminstanz und bietet keinen Ersatz fuer residenten Treiberbesitz.

Die Runtime-Anbindung verwendet deshalb den unten beschriebenen unabhaengig
synchronisierten CPU-Besitz mit eindeutiger Treiber- und Taskgeneration.
Speicher-Metadaten duerfen keine Waits/I/O ueberspannen. Schlafende RM-/NVKMS-
Sperren, IRQ-Metadaten und zeitversetzte Callbacks brauchen getrennte Semantik;
insbesondere bleibt ein RM-Mutex kein IRQ-Owner. Das GSP-Container-Storage
bleibt waehrenddessen beim bereits abgenommenen, einmaligen Init-/Close-Pfad.

Tools/Rm/Adapter.ps1 kopiert den eigenen Quellstand in den privaten Versuch,
baut beide Zielobjekte mit den jeweiligen Original-Komponentenflags und
verlangt genau die zwei privaten Heapimports, keine TLS-/Initialisierer.
Tests/RmMemory.c verwendet reale Hostallokation mit Fehlern und Release-
Canaries, vergleicht Byteoperationen mit der Host-C-Library und prueft
Zugriffsgrenzen mit tatsaechlich unzugaenglichen Randseiten. Unter Linux
x86_64 werden exakt die Freestanding-Zielobjekte ausgefuehrt. Unter Windows
wird derselbe C-Quellstand fuer die Host-ABI gebaut; die Hostobjekte gelangen
nie in das R4OS-Teilobjekt. Die Testallokation ist ausschliesslich hostseitig.

Der frische volle Besitzerlauf besteht in 153,08 Sekunden: 1196 Original-
einheiten, acht Shader, zwei Adapter und beide Links. Der Hosttest besteht
393138 begrenzte Einzelpruefungen; 189 Allokationsaufrufe einschliesslich
sechs Fehlinjektionen, 183 Freigaben und null lebende Puffer. Alle 1196
Originalobjekte stimmen mit dem vorigen Quellbau ueberein; Adapter und
Teilobjekte reproduzieren bytegleich gegen den getrennten Komponentenlauf.
Eine unabhaengige nm-Aufnahme bestaetigt exakt die geaenderte Importmenge.

RM:    14663112 Bytes, 334 offene Symbole, 12501 Definitionen.
NVKMS:  2939472 Bytes,  47 offene Symbole,  2598 Definitionen.
Die zwei privaten Heapimports zaehlen bei beiden Komponenten mit. TLS und
automatische Initialisierer bleiben null. Windows-Hostpruefer als PE fuer
x86_64-windows-gnu cross-kompiliert, dort nicht ausgefuehrt. Der bestehende
NVIDIA-unit-test besteht weiterhin; er behaelt seine 13 Faelle.

Belege: GrafikFirmware07910.json -> os_memory_checkpoint sowie
ExFiles/Reference/GFX/Nvidia/0.79.10/os-memory. Tests.json erfasst den neuen
gezielten Component-Test nur beim expliziten Quellbau. Kein weiterer
Workspace-/Gasttest und kein neues Modul. In diesem Quellbaucheckpoint blieb
NVIDIA.R4D bytegleich: Release 0.79.8, Kernel 0.1.140, NVIDIA 0.1.3. Diese
Versions-/Objektstaende sind historische Quellbaubelege. Der nachfolgende
CPU-Laufzeitcheckpoint aendert Kernel und Treiber; GSP-Start, weitere OS-
Adapter, finale globale Symbolzuordnung und R4M0-Link, HDMI und echte
Hardwareabnahmen bleiben offen. OssiPC wurde nicht kontaktiert.

Residenter Treiberheap mit echtem Worker- und Abschlussweg
---------------------------------------------------------
Kernel 0.1.141 / DriverApi30 erweitert den unveraenderten 600-Byte-Praefix um
heap_query auf 608 Bytes. DriverHeapApi v1/32 liefert allocate/release/stats;
DriverHeapAllocation hat 40 und DriverHeapStats 104 Bytes. Alle Payloads und
zehn Ergebniswerte werden aus dem zentralen Contract erzeugt. Der kontrollierte
Baselinevergleich laesst jeden bisherigen Typ, Wert und sonstigen Schemawert
unveraendert, ausser der ausdruecklichen API-Versionsanhebung 29 auf 30.
SDK-Mindestversion bleibt 24; heap() handelt den optionalen Anhang aus.

Der Loader bindet den CPU-Besitz vor Init an den bereits vorhandenen echten
Treiberstartepoch. Kernel/kernel/driver_heap.zig verwendet den residenten
Kernelheap, waehrend driver_memory_owner.zig nur Metadaten verwaltet. Ein
intrusiver Treap-Eintrag liegt zusammen mit jeder Nutzlast in deren Allokation.
Es gibt keinen separaten festen Allokationspool. Die erwartete Suchzeit ist
logarithmisch; das ist keine harte Worst-Case-Latenz- oder Hardwaremessung.
Alle Groessen-/Paddingadditionen werden vor Allokation auf Ueberlauf geprueft.
Die echte Heapkapazitaet begrenzt den Bestand.

Nur Metadaten und Task-Unwind-Aufnahme laufen unter dem kurzen Runtime-Owner.
Heap-Allokation, echte Freigabe und Nutzdatenzugriffe erfolgen ausserhalb.
IRQ und Storage-Callbacks werden abgewiesen; Init/Shutdown und authentisierte
Driver-Worker duerfen zugreifen. Ein Worker braucht keinen globalen R4D-
Ladeguard, waehrend Init oder Shutdown auf seine Completion warten kann.
Der vorherige Ressourcen-I/O- und GfxDriverMemoryApi-Vertrag wird nicht
umgangen oder durch vorgetaeuschte Programmeigentuemerschaft erweitert.

Eine beim Allozieren beginnende Close-Phase behaelt die fertige Allokation
privat, statt einen Pointer zu veroeffentlichen. Freigabe loest den Indexeintrag
und behaelt Bestands-/Bytezaehler bis zur echten Heapantwort. Fehler setzen
dasselbe Handle wieder ein; Erfolg liest niemals mehr aus dem freigegebenen
Eintrag. Ein Task-Unwind-Token deckt diese geteilten Schritte ab. Ausstehende
Operationen verhindern das Zuruecksetzen des Startepochs. Handles laufen nie
ueber; alte oder fremde IDs finden keinen neuen Besitzer.

Nach erfolgreichem DriverShutdown, Backendfinalizern und IRQ-/Work-/DMA-
Rueckbau gibt der Kernel noch vorhandene CPU-Allokationen frei. Ein endlicher
Durchlauf stoppt beim ersten Freigabefehler; ausstehende oder fehlgeschlagene
Rueckgaben halten den geschlossenen Treiber und das Modul fest. Vom Kernelheap
intern behaltene VM-Endkapazitaet nach erfolgreicher logischer Freigabe ist
Kernelbesitz. Sie ist keine weiterlebende Treiberallokation.

NVIDIA 0.1.4 implementiert r4nv_heap_allocate/free in rm_heap.zig. Ein eigener
16-Byte-Praefix speichert Handle und Korruptionsmarker. Nutzdaten bleiben
16-Byte-ausgerichtet; u64-Laenge und Praefix werden geprueft addiert. Ein
fehlgeschlagenes void-free behaelt den Puffer im echten Kernelbesitz fuer
Retry oder spaetere Ownerbereinigung. Der Marker ersetzt keine Validierung
beliebiger oder bereits freigegebener C-Pointer. Diese CPU-Adressen sind
weder DMA- noch GPU-Adressen; physische Benutzer brauchen eigene nachgewiesene
Lebensdauer und Synchronisation.

Pruefungen: 413 Kernel-Faelle, darunter drei neue Besitzerfaelle im bestehenden
loader_tests-Step und 4096 echte dynamische Eintraege. 14 NVIDIA-Faelle,
darunter der tatsaechliche C-Anbieter mit Hostspeicher, fehlgeschlagenem Free,
Close, alter API und >32-Bit-Laenge. Sieben bestehende/erweiterte Zig-
Ressourcenfassadenfaelle und das zugehoerige C-Programm bestehen; Contract-
Generierung, Kompatibilitaetsreview und Zig/C-Konformanz sind erfolgreich.

Der explizite Modus runtime-check prueft je 64 Allokationen aus Init und einem
echten Driver-Worker, Ausrichtung, Inhalte und veraenderte Freigabereihenfolge.
Ein weiterer Worker erlebt die echte Shutdown-Admission, prueft abgewiesene
neue Allokation und gibt seinen bestehenden Puffer frei. Init endet bewusst
mit -8 vor PCI. Zwei verbliebene Allokationen umfassen 4185 CPU-Bytes:
4096 direkt sowie 73 C-Nutzbytes und 16 Byte Praefix. Der echte generische
Owner-Rueckbau muss beide freigeben. Ein fehlgeschlagener CPU-Pruefpunkt
verhindert nach erwiesener Callback-Ruhe keinen sicheren generischen Rueckbau;
eine ungeklärte physische Nutzung bleibt dagegen ein Entladeveto.

Der SMP4/KVM-Lauf besteht in 20,31 Sekunden mit Kernel 0.1.141, frischem
2-GB-Testmedium, 1 GB RAM und ohne Gastnetz/NVIDIA-Geraet. Die generische
Kernelmeldung bestaetigt cpu-heap=2, cpu-bytes=4185, result=complete, gefolgt
von nutzbarem bootfb und regulaerem Poweroff. Der erste Versuch bestand die
CPU-/Shutdown-Probe, scheiterte aber am fehlenden Hostmarker: Die Bootlog-
Freigabezeile hatte keinen Treibernamen und fiel aus DISPLAYD /NVIDIA heraus.
Sie wird nun mit dem Registry-Namen als eine vollstaendige begrenzte Zeile
geschrieben; die Freigabeanforderung im Hosttest wurde nicht abgeschwaecht.
Die normale passive Variante besteht mit demselben Kernel und NVIDIA.R4D
in 20,22 Sekunden, einschliesslich fehlender GPU, erfolgreichem Shutdown und
weiter nutzbarem bootfb. Beide Laufzeiten enthalten den kompletten Gastboot;
sie sind keine Allokations- oder Grafikleistungsmessungen.

Belege: GrafikFirmware07910.json -> driver_heap_checkpoint und das getrennte
Archiv ExFiles/Reference/GFX/Nvidia/0.79.10/driver-heap. Der vorhandene
SMP4-Grafiklauf hat die explizite Variante nvidia-runtime; normale Profile und
die all-Auswahl bleiben unveraendert. Kein neues Modul, keine GPU-Initialisierung,
kein kombinierter RM-Link, kein OssiPC-Kontakt. 0.79.10 bleibt offen; der
Release bleibt 0.79.8, solange keine weitere Unterversion abgeschlossen ist.

Monotone Uhr: echter R4D-Anbieter und vier OS-Adapter (10.09.2026)
----------------------------------------------------------------
Kernel 0.1.142 / DriverApi31 erweitert den unveraenderten 608-Byte-Praefix
um monotonic_clock auf 616 Bytes. Der vorhandene feste MonotonicClockInfo-
Payload bleibt bei 80 Bytes. Ein gemeinsamer Besitzer kernel/monotonic_api.zig
bildet die kanonische Uhr fuer R4SYS und R4D ab. Es entsteht weder eine zweite
Zeitquelle noch eine neue Timer-/Schedulerpolicy. SDK prueft Version, Groesse
und optionalen Slot; alte Treibertabellen und der Heapvertrag bleiben gueltig.

NVIDIA 0.1.5 bindet den echten rm_clock.zig-Anbieter vor seinen Workcallbacks.
Haeufige Zeitstempel lesen den erfassten now_ns-Eintrag aus DriverResourceApi
ohne Lifecycle-/Ressourcen-Guard und ohne Berechnung kompletter Uhrmetadaten.
Aufloesungsabfragen lesen aktuelle Metadaten mit Quellgeneration und Qualitaet.
TSC, HPET und degradierter periodischer Fallback behalten dieselbe ns-Einheit;
IRQ-unabhaengige Fortschrittsgarantie folgt nur aus dem entsprechenden Flag.
Fehlende/falsche Uhrdaten verriegeln den Anbieter bis zur naechsten Bindung.
UINT64_MAX bleibt Fehler, niemals erfundener Nullzeitpunkt.

os_get_current_tick und os_get_current_tick_hr liefern beide monotone
Nanosekunden; os_get_tick_resolution liefert das aktuelle Quellenquantum in
Nanosekunden, nicht die Frequenz. nvkms_get_usec teilt nur gueltige Werte durch
1000 und erhaelt den Fehlerwert. Originalprototypen und Semantik stammen aus
den unveraenderten OS-Interface-Headern und kernel-open-Referenzen 570.144.
Kalenderzeit, CPU-Frequenz, Sleep/Yield, Mutex und Semaphore bleiben offen.
Die wertliefernden Upstream-Uhrfunktionen koennen keine beliebige RM-Schleife
abbrechen. Spaetere native Dispatches muessen den Fehler beachten und eigene
begrenzte Arbeitsschritte besitzen; diese Anbindung ist noch nicht vorhanden.

Der volle Quellbau besteht in 150,04 s. Alle 1196 Originalobjekte bleiben
bytegleich zum CPU-Speicherstand. Vier Adapter und beide Teilobjekte sind
gegen einen separaten Integrationslauf reproduziert; GNU nm bestaetigt die
genaue Importdifferenz. Die zwei Teilobjekte bleiben getrennt von NVIDIA.R4D:

RM:    14663360 Bytes, 333 offene Symbole, 12504 Definitionen.
NVKMS:  2939920 Bytes,  47 offene Symbole,  2599 Definitionen.

Im RM stehen vier private Heap-/Uhrimporte, in NVKMS drei; die echten Anbieter
sind im R4D implementiert, aber der kombinierte Link fehlt weiterhin. TLS und
automatische Initialisierer bleiben null. os-adapter-results.json Schema 2
erfasst die beiden Speicher- und die beiden Zeitobjekte mit getrennten
Hostnachweisen. 393138 Speicherpruefungen bleiben erfolgreich. Die 42 neuen
Uhrpruefungen verwenden die exakten Zielobjekte unter Linux und decken Null,
Einheitengrenzen, Werte oberhalb 32 Bit, UINT64_MAX-1, Fehler und Aufloesung ab.
Der Windows-C-Checker ist crosskompiliert, aber nicht auf Windows ausgefuehrt.

413 Kerneltests, 15 NVIDIA-Faelle, acht Zig-Fassadenfaelle samt C-Fassade und
13 Contract-Bauschritte bestehen. Der kontrollierte Schema-/Baselinevergleich
aendert ausschliesslich DriverApi30 zu 31; keine bestehenden Payloadfelder
oder sonstigen Konstanten aendern sich. Das vorhandene SDK-Heappraefix-Test
prueft nun dessen feste Endposition statt die gewachsene Gesamttabellengroesse.

SMP4/KVM runtime-check besteht in 20,24 s: je 64 monotone Reads im echten Init
und Worker, Fortschritt nach Scheduler-Wait, weiterhin 64 CPU-Allokationen
je Kontext und erfolgreiche Close-/Worker-/Restpufferbereinigung. Der separate
passive SMP4-Fallback besteht in 20,22 s. Beide nutzen frische 2-GB-Images,
1 GB RAM, kein Gastnetz und kein NVIDIA-Geraet. Die Zeiten beschreiben den
Gastablauf und sind kein Benchmark fuer Uhr, Allokator oder Grafik.

NVIDIA.R4D bleibt bei 49152 allokierten Imagebytes (48 KB); vier eingebettete
Originalressourcen sind erneut vollstaendig gehasht. Produktartefakte,
Quellobjekte, Importdifferenz, Gastimages und Logs sind in clock_checkpoint
und ExFiles/Reference/GFX/Nvidia/0.79.10/clock nachvollziehbar erfasst.
RELEASE bleibt 0.79.8; OssiPC wurde nicht kontaktiert. 0.79.10 bleibt offen.

Dedizierte R4D-Tasks und echte SMP-Ausfuehrung (10.09.2026)
---------------------------------------------------------
Kernel 0.1.143 / DriverApi32 fuegt thread_query bei Offset 616 hinzu;
Gesamtgroesse 624 Bytes. Alle vorhandenen Felder und Konstanten bleiben
unveraendert, mit Ausnahme der ausdruecklichen API-Versionserhoehung.
Vier neue Payloads besitzen 32/64/96/72 Bytes. SDK bietet typisierte Zig-
und C-Fassaden; der kontrollierte Contract-/Baselinevergleich besteht.

kernel/driver_threads.zig ist ein generischer Scheduler-/Lebensdauerbesitzer,
keine NVIDIA-Implementierung im Kernel. driver_thread_owner.zig verwaltet
dynamische residente Records mit intrusive Treap, exakter R4D-Startepoche
und nicht umlaufenden Handles. Kein neuer fester Threadpool. Jeder echte
Task erhaelt die vorhandenen 64-KB-Kernelstacks mit 16-KB-Guard und den vollen
Modul-FPU-Zustand. Ausfuehrungsbesitz und Kill-Schutz stehen vor Registry-
Publikation fest. BSP ist Standard; SMP benoetigt ausdrueckliche Freigabe
durch den Treiber. Die bestehenden Schedulerrollen bleiben unveraendert.

Die neue Schnittstelle nimmt keinen globalen R4D-Lifecycle-Guard. Ein solcher
Guard darf weiterhin beim wartenden Init-/Shutdown-Aufrufer liegen; die
dedizierten Callbacks koennen unabhaengig fortschreiten. Kurze Runtime-
Abschnitte schuetzen nur Metadaten und Wait-Anmeldung. Heap-, Task-, Stack-
und FPU-Arbeit, Modulaufrufe und Warten liegen ausserhalb dieser Abschnitte.
Die kleine Storage-Callback-Besitzprojektion ist ebenfalls synchronisiert,
damit SMP-Aufrufer deren bestehenden Ausschluss verlaesslich pruefen.
Treiberlogs veroeffentlichen komplette begrenzte Zeilen mit echtem Besitzer.

Start/Stop/Join/Release/Status/Current/Sleep/Stats sind an den wirklichen
Treiberkontext gebunden. Join ist endlich; null pollt, UINT64_MAX ist verboten.
Ein gestoppter Aufrufer kann seinen eigenen Join abbrechen, ohne den Zieltask
zu stoppen. Sleep verwendet logische Ticks; null yieldet, UINT64_MAX wartet
bis zum kooperativen Stop. Geweckt werden ausschliesslich eigene Waits des
Threaddienstes, niemals beliebige Kernel-I/O- oder Lock-Waits. Kein harter
Callback-Abbruch und keine Mindestdauer in Nanosekunden werden behauptet.

Owner-Close sperrt neue Starts und weckt diese Waits. Alle Modulcallbacks
und laufenden Anlegevorgaenge muessen vor Backend-/IRQ-/Work-Abbau beendet
sein. Anschliessend gehen exakte Task-/Stack-Freigabe und Record-Freigabe
dem DMA-/CPU-Heap-Abbau voraus. Abgeschlossene Callbacks koennen noch einen
Kernel-Epilog besitzen; ein erfolgreicher Join bedeutet deshalb nicht, dass
release sofort erfolgreich sein muss. Ein gemeinsames 1000-Tick-Budget
begrenzt generische Retirement-Retries. Freigabefehler behalten Record und
Treiber; Close-Rennen und Fehler vor Publikation bleiben privat besitzend.

416 Kerneltests bestehen, darunter drei neue Besitzerfaelle mit echten
4096 Records, Start-/Close-Rennen, Join-Leases und fehlgeschlagener Freigabe.
15 NVIDIA-Faelle, neun Zig-Fassadenfaelle mit C-Ausfuehrung und 13 Contract-
Bauschritte bestehen. Die C-Fassade ist zusaetzlich fuer Windows x86_64
crosskompiliert; sie wurde nicht auf einem Windows-Host ausgefuehrt.

Der echte SMP4/KVM-runtime-check besteht in 20,22 s. Vier dedizierte
Callbacks laufen mit CPU-Maske 0xf tatsaechlich auf allen vier CPUs,
waehrend die alte gemeinsame BSP-Workqueue noch belegt ist. Sie pruefen
Task-/Epochenidentitaet, CPU-Heapinhalt und monotone Zeit. Das ist ein
Fortschritts- und Integrationsnachweis, kein Durchsatz-/Grafikbenchmark.
Die Probe verlangt mindestens zwei CPUs, keine feste Vier-CPU-Affinitaet.

Selbst-Join/Selbst-Freigabe, Timeout-Poll, noch aktiver Zieltask, gezielter
Join-Abbruch, kooperativer Stop und alte Handles werden im echten Dienst
geprueft. Owner-Close weckt einen weiteren Task, sperrt neue Starts und
Allokationen und erlaubt die Freigabe seines vorhandenen CPU-Puffers.
Generisches Cleanup entfernt danach drei behaltene Taskrecords und die
bisherigen zwei CPU-Allokationen mit 4185 Payloadbytes. Die bisherigen
64 Init-/64 Worker-Heap- und Uhrproben bleiben erfolgreich.

Der separate passive SMP4-Fallback besteht in 20,21 s. Beide Gaeste nutzen
frische 2-GB-Images mit 1 GB RAM, ohne Gastnetz und ohne NVIDIA-Geraet.
bootfb/DISPLAYD und regulaeres Poweroff bleiben erfolgreich. NVIDIA.R4D
belegt jetzt 57344 allokierte Imagebytes (56 KB); alle vier unveraenderten
Paketressourcen wurden erneut vollstaendig gehasht. Nachweise und Artefakt-
Hashes liegen in driver_threads_checkpoint sowie unter
ExFiles/Reference/GFX/Nvidia/0.79.10/driver-threads.

Der neue Ausfuehrungsbesitz gilt fuer CPU-Heap, Threaddienst und Logs; die
bereits unabhaengige Uhr bleibt nutzbar. Alte PCI-/DMA-/Backend-/Dateipfade
erhalten dadurch keine pauschale SMP-Freigabe. Die Original-RM/NVKMS-Teil-
objekte und bisherigen C-Adapter sind nicht geaendert; ihre 333/47 offenen
Symbole bleiben Bestandteil des vorherigen Quellbau-Nachweises. Noch fehlen
RM-Warte-/Synchronisations-/Threadadapter, kombinierter Link, GSP/RPC-Boot
und Hardwareabnahme. RELEASE bleibt 0.79.8; 0.79.10 bleibt offen. OssiPC
wurde nicht kontaktiert, und keine GPU-Ausfuehrung wird aus CPU-Tests abgeleitet.

Residente R4D-Semaphore und fruehere SMP-Deadlines (10.09.2026)
--------------------------------------------------------------
Kernel 0.1.144 / DriverApi33 fuegt semaphore_query bei Offset 624 hinzu;
Gesamtgroesse 632 Bytes. Drei Payloads mit 40/80/64 Bytes und 15 Konstanten
kommen hinzu; alle alten Felder bleiben unveraendert. Typisierte Zig-/C-
Fassaden pruefen optionale Felder und erhalten die volle Zaehler-/Handle- und
Timeoutbreite. RELEASE bleibt 0.79.8, NVIDIA ist 0.1.7.

Der generische Kernelbesitzer verwendet echte residente Heaprecords mit
Treap-Index und exakter Treiberstartepoche. Wait-Leases und Task-Unwindschutz
halten das Objekt bis zur Rueckkehr jedes wartenden Aufrufs. Allokation,
Freigabe und Warten liegen ausserhalb kurzer Runtime-Metadatenabschnitte.
Fehlgeschlagene Freigabe behaelt dieselbe Identitaet; erfolgreiche Freigabe
liest kein freigegebenes Record. Es gibt keinen festen Semaphorepool.

Der vorhandene Scheduler-Semaphorepfad uebergibt genau ein Permit an den
naechsten Waiter. Null-Timeout prueft sofort, endliche Ticks verwenden eine
Deadline, UINT64_MAX wartet auf ein echtes Permit. Neue Anlage schliesst
beim Owner-Close; vorhandene Operationen bleiben fuer Shutdown nutzbar.
Stop oder Close gelten niemals als erfolgreicher Erwerb. Ein Zaehler besitzt
keinen Task-Mutexhalter; der Treiber muss alle Benutzer vor Destroy beenden.
Generisches Cleanup folgt IRQ-/Work-/Task-Quieszenz und geht DMA/CPU-Heap voraus.
Try und Einzel-Release sind resident und IRQ-faehig; echte Geraete-IRQ-Abnahme
steht aus. Der private C-Anbieter und originale RM-/NVKMS-Adapter fehlen noch.

Die Konkurrenzprobe deckte einen bestehenden SMP-Fehler auf: APs konnten eine
fruehere Deadline anmelden, waehrend der BSP bereits auf eine spaetere
One-Shot-Deadline wartete. Ein diagnostischer Gast erreichte nur 22 geschuetzte
Aenderungen bis zum unveraenderten Fuenf-Sekunden-Join-Timeout (-8). Der
kontrollierte Rueckbau und bootfb blieben dabei erhalten. blockCurrent sendet
nun bei einer frueheren endlichen AP-Deadline den vorhandenen Reschedule-IPI
an den BSP. Dessen bestehender STI/HLT-Pfad prueft das aktuelle Minimum neu;
APs programmieren weder HPET noch den Timer des BSP. Kein Polling-Ersatz.

Mit dieser Korrektur besteht dieselbe Probe: drei FIFO-Waiter, vier Tasks mit
256 geschuetzten Aenderungen, Timeout, Ueberlauf, alte Handles und Busy-Destroy.
Ein gestoppter Task bleibt beim echten Owner-Close bis zur Shutdown-Freigabe
an seiner Semaphore wartend. Danach entfernt generisches Cleanup zwei
Semaphore, die bisherigen drei Taskrecords und zwei CPU-Puffer (4185 Bytes).
Die bestehenden Heap-/Uhr-/Threadproben bleiben erfolgreich. Der Runtimegast
besteht in 20,72 s, der passive Fallback in 20,32 s; jeweils SMP4/KVM, 1 GB RAM,
frische 2-GB-Images ohne Gastnetz oder NVIDIA-Geraet. Das sind Ablaufzeiten,
keine Grafik-/Treiberbenchmarks. Auch der vorhandene kurze SMP4-/Clocklauf
besteht mit dem neu gebauten Kernel. Dabei wurde eine vorhandene Luecke in
der Testkonfiguration behoben: Der Pflichtmarker NVMEIRQ verlangte ein
Geraet, obwohl der Starter nur SATA konfigurierte. Headless-Testlaeufe
erhalten jetzt eine virtuelle NVMe mit frischer 16-MB-Testnamespace; der
interaktive SATA-Standard bleibt bestehen. MSI/MSI-X-IRQ und asynchroner
Blockabschluss werden damit wieder tatsaechlich ausgefuehrt.

419 Kerneltests, 15 NVIDIA-Faelle, zehn Zig-Fassadenfaelle mit C-Ausfuehrung
und 13 Contract-Bauschritte bestehen. Der Windows-C-Checker ist lediglich
crosskompiliert. Der Besitzerindex deckt unter anderem 4096 echte Records,
Close-Rennen und behaltene Freigabefehler ab. Die vier Originalressourcen im
R4D wurden erneut vollstaendig gehasht; bisherige C-Adapter, private Heap-/
Uhr-Anbieter und der Originalquellbau bleiben unveraendert. Ein neuer RM-Link
oder GSP-/HDMI-Erfolg wird daraus nicht abgeleitet. Belege einschliesslich
negativer Deadline-Probe: semaphores_checkpoint und
ExFiles/Reference/GFX/Nvidia/0.79.10/semaphores. OssiPC wurde nicht kontaktiert;
0.79.10 und alle betreffenden Hardwareabnahmen bleiben offen.

Privater Semaphore-Anbieter und originale C-Adapter (10.09.2026)
--------------------------------------------------------------
NVIDIA 0.1.8 verbindet fuenf private C-Signaturen mit den vorhandenen echten
R4D-Semaphoren und dem residenten CPU-Heap. Kernel 0.1.144, DriverApi33 und
RELEASE 0.79.8 bleiben unveraendert. Es entsteht kein neues Modul oder Gate.

Opake Zeiger besitzen echte 16-Byte-CPU-Boxen samt 16-Byte-Heap-Praefix und
vollstaendigem u64-Semaphorehandle. Der volle u32-Zaehler bleibt erhalten.
Null-Timeout prueft sofort; UINT64_MAX wartet auf ein echtes Permit. Der
Aufrufer muss vor Free alle Benutzer beenden. Cookies erkennen beschaedigte
Metadaten eines gueltigen Live-Zeigers, keine beliebigen oder alten Adressen.
Ein fehlgeschlagenes Destroy behaelt beide Ressourcen. Ist die Semaphore
bereits entfernt, aber die CPU-Freigabe fehlgeschlagen, wiederholt ein Retry
nur die CPU-Freigabe. Der Heap liefert intern den einzelnen Freigabeausgang;
sein vorhandener void-C-Vertrag bleibt gleich. Unerwartete Anbieterfehler
verriegeln neue Nutzung bis Rebind; bestehender Rueckbau bleibt erreichbar.

Zwoelf RM- und vier NVKMS-C-Einstiege bauen gegen die gepinnten Originalheader.
MaySleep/ISR und der Unterschied zwischen bedingtem Mutex- und Semaphore-
Erwerb folgen der Referenz. Das ist keine Spinlock-Implementierung. void-
Free/Up/Down benoetigen bei Fehler den nicht zurueckkehrenden Zielimport
r4nv_native_fault. Dessen echte native Dispatch-/Quieszenzgrenze fehlt noch;
es gibt keinen Ziel-Stub und keinen erfundenen Erwerb. Der Hostchecker nutzt
setjmp/longjmp ausschliesslich fuer seine kontrollierten Fehlerpruefungen.

Der explizite Sourcebuild fuehrt 180 Semaphorepruefungen mit den echten
freestanding C-Objekten aus, darunter 25 nicht zurueckkehrende Fehlerpfade.
Die vorhandenen 393138 Speicher- und 42 Uhrpruefungen bleiben erfolgreich.
Ein unabhaengiger zweiter Quellbau reproduziert alle 1196 Originalobjekte,
sechs Adapter und beide partiellen Links bytegleich. Alle vier vorherigen
Speicher-/Uhradapter und acht Shaderobjekte entsprechen ihrem alten Nachweis.
Die exakte Aenderung der offenen Symbolmenge wurde zusaetzlich mit GNU nm
verglichen. Der Windows-C-Checker wurde crossgebaut, nicht ausgefuehrt.

RM verbleibt mit 329 offenen Symbolen, NVKMS mit 48. Dazu gehoeren zehn
beziehungsweise acht private Importe einschliesslich der nativen Fehlergrenze.
Die einzige gemeinsame globale Doppeldefinition bleibt nvstatusToString.
TLS und automatische Initialisierer bleiben null. Die bisherige Original-
Version, Quellen, Shader, Firmware und Lizenzen wurden nicht geaendert.

Siebzehn normale NVIDIA-Faelle bestehen. Zwei neue Anbieterfaelle verwenden
reales Host-Backing und simulierte Diensttabellen fuer Fehler, Close und
Integerbreiten. Der echte SMP4/KVM-runtime-check prueft den privaten Zig-
Anbieter getrennt von diesen C-Objekten: vier Tasks, 128 geschuetzte Updates
mit Scheduler-Wait unter gehaltenem Permit, endlicher Timeout, Busy-Free und
Stop ohne Permit. Der Shutdown liefert das echte Permit und gibt danach
Box, Semaphore und Task frei. Alle vorherigen Proben mit drei FIFO-Waitern,
256 Konkurrenzupdates sowie Heap, Uhr und Tasks bestehen ebenfalls.
Generisches Cleanup entfernt weiterhin drei Taskrecords, zwei Semaphore und
zwei CPU-Puffer mit 4185 Bytes. Vorhandener bootfb/DISPLAYD bleibt nutzbar.

Der Runtimegast besteht in 21,23 s, der passive Fallback in 20,22 s;
beide SMP4/KVM, 1 GB RAM, frische 2-GB-Images ohne Gastnetz oder NVIDIA-Geraet.
Das sind Ablaufzeiten, keine Grafikbenchmarks. Das R4D belegt
69632 allokierte Imagebytes (68 KB); alle vier
unveraenderten Paketressourcen wurden erneut vollstaendig gehasht. Der
unveraenderte Kernel-/API-Stand behaelt seinen vorherigen 419-Test-Nachweis;
dieser Zwischenstand wiederholt keine unveraenderten Kernel-Gates.

Belege: rm_semaphores_checkpoint und
ExFiles/Reference/GFX/Nvidia/0.79.10/rm-semaphores. Der erste C-Baufehler
fehlender NULL-Deklaration ist separat dokumentiert; erfolgreiche Laeufe
verwenden den korrigierten eigenen Adapter. OssiPC wurde nicht kontaktiert.
Die originale C-Ausfuehrung im Gast, native Fehlergrenze, kombinierter Link,
weitere OS-/Kryptofunktionen, GSP-Boot und HDMI-/IRQ-Hardwareabnahme bleiben
offen. Keine Unterversion oder Hardwareabnahme wird damit abgeschlossen.

Echter C-Speicher-/Uhrpfad im NVIDIA-Modul (10.09.2026)
----------------------------------------------------
NVIDIA 0.1.9 verbindet alle 21 eigenen RM-/NVKMS-C-Adapter fuer Speicher,
Strings und Uhr mit ihren vier vorhandenen privaten Zig-Anbietern. Die
Calls laufen im fertigen R4D ueber reale DriverApi-Dienste. Kernel 0.1.144,
DriverApi33 und RELEASE 0.79.8 bleiben unveraendert. Die Unterversion bleibt
offen; OssiPC wurde nicht kontaktiert.

Der SDK-Manifestparser erlaubt nun eine Zig-Rootquelle mit nachfolgenden
C-SOURCE-Eintraegen auch fuer R4D, neben dem bestehenden R4L-Pfad. Geordnete
Quellen, Include-Wurzeln, Defines und Flags gehen in denselben ELF-Bau und
R4M0-Packager. Treiber-Entry, Import und Metadaten bleiben erhalten; Zig-
R4X/R4P behalten eine Quelle. Es entsteht weder ein zweites Modul noch eine
neue Runtime-Abhaengigkeit. R4BUILD bekommt dadurch keine Zig/C-R4D-Faehigkeit.

Neunzehn originale MIT-Header, 236096 Bytes, sind bytegleich aus dem bereits
gepinnten NVIDIA-570.144-Referenzbestand uebernommen. ORIGIN.json nennt alle
Pfade/Hashes und denselben Firmware-/Quellpin. Ein gemeinsamer PS7-Schritt
prueft den ganzen Bestand vor C-Kompilation. Gueltiges Paket, geaenderte,
fehlende, zusaetzliche und zu einem anderen Pin gehoerende private Kopien
wurden geprueft; nur das gueltige Paket wird zugelassen. Originale blieben
unveraendert. Das ist eine notwendige normale Eingangspruefung des neuen
Fremdmaterials, kein neuer eigenstaendiger Workspace-Abnahmelauf.

Saemtliche vollstaendigen Header-Copyright-/Lizenzhinweise und das komplette
originale COPYING bleiben erhalten. LICENSES.txt liegt bytegleich als
NVIDIA-570.144-HEADERS-LICENSE.txt in der nicht allokierten R4D-Ressource,
im Distributionsdateisystem und daneben unter Legal. Beide originalen GSP-
Container, deren Lizenz und NVFW-LOCK.json bleiben unveraendert; nun sind
es fuenf Ressourcen. Alle fuenf wurden erneut vollstaendig gehasht. Die
realen NTFS-Dateien beider gebauten Testimages wurden zusaetzlich gelesen:
Treiber und neue Lizenz stimmen mit den kanonischen Dateien ueberein.

Sechzehn Manifesttests und alle siebzehn NVIDIA-Besitzerfaelle bestehen.
Der gezielte normale SDK-Bau besteht mit 24 Schritten. Ein anfangs veraltetes
installiertes ModuleCatalog hatte den Imageplan vor dem Gaststart abgelehnt;
der regulaere SDK-Neubau aktualisiert den Parser auch dort. Der erste rote
Versuch ist separat erhalten und zaehlt nicht als Gastnachweis. Eine erste
temporaere Fehlermeldungspruefung musste zudem an PS7-Zeilenumbrueche angepasst
werden; die tatsaechliche Headerablehnung hatte bereits funktioniert.

Die echte ELF-Datei ist ueber den protokollierten Packageraufruf mit dem
installierten Container verbunden. Alle 21 Adapter und vier Anbieter sind
definiert; die C-Probe ruft alle Adapter auf und diese die echten Anbieter.
Keine offenen ELF-Symbole, TLS-Sections oder automatischen Initialisierer.
Der C-Teil benutzt keine SIMD-Register und setzt das Direction Flag nicht.
Die oeffentlichen R4M0-Importe/Exporte bleiben ein R4DEV-Import und die zwei
Treiber-Einstiege. Das fertige R4D besitzt 1196 Relocations, nur ABS64/REL32.

Der vorhandene runtime-check fuehrt cpu_probe.c sowohl im Init als auch in
einem echten DriverWork aus: Null-/Grenzallokation, 16-Byte-Alignment,
Zero-Fill, unausgerichtete Kopien mit Randmarken, ueberlappende Moves,
Strings und ns-/us-Uhrumrechnung. Erst beide echten Aufrufwege und null
lebende Allokationen geben native-c=OK adapters=21 link=actual aus. Die
vorherigen Semaphore-, Thread-, Heap- und Uhrproben bleiben erfolgreich.
Shutdown gibt nach Callback-Ende weiterhin drei Taskrecords, zwei Semaphore
und zwei CPU-Puffer mit zusammen 4185 Bytes generisch frei. Kein GPU- oder
IRQ-Hardwarenachweis wird aus dieser CPU-Ausfuehrung abgeleitet.

Runtimegast: 21,23 s; passiver Abwesenheits-/Fallbackgast: 20,24 s. Beide
SMP4/KVM mit 1 GB RAM, frischem 2-GB-Image, ohne NVIDIA-Geraet und Gastnetz.
bootfb/DISPLAYD und regulaeres Ausschalten funktionieren. Ablaufzeiten sind
keine Grafikbenchmarks. Das Modul hat 92273327 Dateibytes und unveraendert
69632 allokierte Imagebytes (68 KB); Firmware/Lizenzen bleiben dateigebunden.
Kernelartefakt und frueherer 419-Test-Nachweis bleiben unveraendert.

Die sechzehn C-Semaphoreadapter bleiben im separaten Original-Sourcebuild.
Ihre nicht zurueckkehrende native Fehlergrenze hat noch keinen Zielanbieter.
Der komplette RM-/NVKMS-Link, weitere OS-/Kryptofunktionen, GSP-Authentifizierung,
GPU-Start, native Anzeige und HDMI-/IRQ-Abnahme bleiben offen. Kein Erfolgs-
Stub ersetzt diese Arbeit. Neue Module: null; Tests-Inventar: 710 Eintraege,
Modulinventar: 147. Zwei bestehende Abnahmeeinstiege decken den neuen Pfad ab;
cpu_probe.c und die notwendige Headerpruefung sind im Inventar erfasst.

Belege: native_c_checkpoint und
ExFiles/Reference/GFX/Nvidia/0.79.10/native-c, einschliesslich der genauen
ELF-Datei, Symbol-/Disassemblypruefung, Paketfehler, Image- und SMP4-Protokolle.
Alle frueheren Checkpoints bleiben als historische Nachweise unveraendert.

Native Semaphore-C-Aufrufe und kontrollierter Selbstabbruch (10.09.2026)
--------------------------------------------------------------------
NVIDIA 0.1.10 verbindet alle 37 eigenen Speicher-, String-, Uhr- und
Semaphore-C-Adapter mit zehn echten privaten Anbietern. Alle sechzehn
Semaphore-C-Einstiege laufen nun im fertigen R4D. Kernel 0.1.145 stellt
dafuer einen expliziten, synchronen Selbstabbruch dedizierter R4D-Tasks
bereit. DriverApi33/632 und RELEASE 0.79.8 bleiben erhalten. Die Unterversion
und ihre Hardwareabnahmen bleiben offen; OssiPC wurde nicht kontaktiert.

Die versionierte optionale DriverThreadApi behaelt Version 1 und ihren
vollstaendigen 72-Byte-Praefix. abort_current folgt bei Offset 72; die neue
Tabelle hat 80 Bytes. Query schreibt fuer Kapazitaeten 72 bis 79 genau 72
Bytes und ab 80 genau 80 Bytes. Zig und C pruefen die Slotverfuegbarkeit;
ein Start mit Abort-Flag ohne entsprechenden Dienst wird vor dem Aufruf
abgelehnt. Die kontrollierte Contract-Baseline-Aenderung betrifft diese
angehaengte Funktion, ihre neue Defaultkapazitaet, drei Konstanten und
beschreibende Flags. Alle anderen Layouts und bisherigen Felder bleiben
erhalten. Vorheriger normaler Generatorlauf hatte den Defaultwechsel
korrekt zur ausdruecklichen Baselinepruefung angehalten.

Startflag 2 installiert einen pro echtem Task auf dessen Stack lebenden
Aufrufrahmen. Statusflag 4 zeigt einen angenommenen Abbruch an und ist
als Startflag ungueltig. Nur derselbe authentifizierte Task darf mit einem
negativen Ergebnis zurueckkehren. IRQ, deaktivierte Interrupts, Kernel-
Locks, Runtime-Sektionen, Wait-Leases und weitere Unwind-Guards verhindern
den Transfer. Der initiale Guard bleibt bis zum normalen Taskabschluss
erhalten. Keine fremde Task-ID, CPU-Index-Vermutung oder feste Frame-Tabelle.

Die originale R4OS-Assembly in arch/x86_64/callback_abort.zig stellt den
SysV-Callerstack, dessen callee-saved GPRs sowie MXCSR-/x87-Steuerzustand
wieder her und loescht DF. Es ist eine regulaere Aufruf-/Rueckkehrgrenze;
kein als returns-twice angenommener Zig-Setjmp und keine fremde kopierte
Implementierung. Die gepinnte primaere AMD64-ABI-Referenz samt originaler
Herausgeber-/Lizenzangabe liegt unter
ExFiles/Reference/GFX/SysV-AMD64-ab2062ad. Vollstaendiger CC-BY-4.0-Text
aus dem offiziellen Creative-Commons-Repository liegt daneben; Hashes
und beide festen Quellrevisionen sind dokumentiert.

Modul-Defers und fremde Unwind-Handler werden nicht ausgefuehrt. Der native
Besitzer muss alle Speicher-, Lock-, Semaphore- und DMA-Verpflichtungen
explizit verfolgen. Erst das Ruhen aller beteiligten Aufrufe erlaubt die
Freigabe. Die Kernelgrenze faengt weder CPU-Exceptions noch endlose Schleifen
oder beliebige GPU-Fehler ab. Abgelehnte Plattformaufrufe kehren mit Status
zurueck. Die private void-C-Fehlerfunktion ausserhalb ihrer vereinbarten
abortierbaren Grenze ist eine ABI-Verletzung und trappt; ein angenommener
nativer Fehler erreicht diesen Zweig nicht. Keine erfundene Semaphore-
Freigabe, endlose Ersatzparkung oder erzwungene fremde Taskbeendigung.

rm_native nimmt caller-eigene Invocations bis zur Taskfreigabe entgegen,
prueft die Zulassung vor Start und erneut vor Eintritt und speichert den
ersten Fehler atomar. Neue normale Aufrufe werden dann abgelehnt; bereits
eingetretene Peers muessen kooperativ fertig werden. Explizite Cleanup-
Aufrufe bleiben moeglich, ohne den Fehler zu loeschen. Es gibt weder einen
festen Invocation-Pool noch eine globale native Serialisierung.

Die vorhandene Runtimeprobe prueft im echten Kernel Kapazitaeten 72, 73,
79 und 80 mit unveraenderten Randbytes, falsche Version/71-Byte-Kapazitaet,
Init-/nicht-abortierbaren Kontext, nichtnegative Ergebnisse und Statusflag
als ungueltiges Startflag. Danach nutzt sie alle sechzehn C-Adapter im
gesunden Betrieb mit null verbleibenden Ressourcen.

Ein C-Waiter blockiert anschliessend wirklich auf einer Null-Semaphore;
die Kernelstatistik bestaetigt seine Einschreibung. Ein zweiter C-Aufruf
versucht Busy-Free. Der reale private Anbieter behaelt Semaphore und
CPU-Box; die void-C-Schicht ruft r4nv_native_fault auf. Der Task endet
mit -76001/Abbruchflag, sein Folgebefehl bleibt unerreicht. Zwei Allokationen
mit 145 Bytes inklusive privater Praefixe und der Waiter bleiben erhalten;
Join ohne Wartezeit meldet weiterhin Timeout. Ein weiterer normaler nativer
Start wird abgewiesen.

Erst ein expliziter Cleanup-C-Aufruf liefert einen echten Permit. Der
Waiter beendet sich, wird gejoint und samt exakter Taskgeneration freigegeben.
Ein weiterer C-Aufruf prueft 97 Speicher-Canarybytes und gibt die Semaphore
frei. Der native Besitzer gibt danach den Speicher mit der statusfaehigen
privaten Heapfunktion frei. Alle Zaehler erreichen die vorherige Basis;
der native Fehler bleibt gespeichert. Teilweise fehlgeschlagene Proben
werden nach Owner-Close ohne neue Tasks geordnet beendet; keine Freigabe
bei unklarer Peer-Ausfuehrung. Der bisherige private Semaphoreanbieter
bleibt verfuegbar, weil Busy ein erwartetes Retry und kein Anbieterdefekt ist.

Direkt gespeicherte C-Funktionsadressen erzeugten zunaechst
REX_GOTPCRELX-Hinweise, die der Packager korrekt abwies. Kleine Zig-
Taskeinstiege rufen die echten C-Funktionen jetzt direkt auf. Es wurde
keine Relocationpruefung gelockert. Das verpackte ELF definiert alle
37 Adapter und zehn Anbieter; die Proben rufen die echten C-Symbole auf.
Keine offenen ELF-Symbole, TLS oder automatischen Initialisierer. Der
C-Teil verwendet keine SIMD-Register und setzt DF nicht. R4M0 bleibt bei
1410 Relocations (83 ABS64, 1327 REL32), einem Import und zwei Exports.
Das Modul hat 92285616 Dateibytes und 77824 allokierte Imagebytes (76 KB).
Alle fuenf nicht allokierten Ressourcen, 19 Originalheader und vollstaendigen
Notices sind bytegleich. Beide realen NTFS-Treiber-/Lizenzdateien und
danebenliegende Legal-Dateien stimmen mit den kanonischen Dateien ueberein.

421 Kerneltests einschliesslich zwei Architekturfaellen, 13 Contract-
Buildschritte, zehn Zig-Fassadenfaelle plus C-Ausfuehrung und alle 17
NVIDIA-Besitzerfaelle bestehen. Die bestehenden Architektur-/C-Fassaden-
Quellen wurden auch fuer Windows kompiliert, dort nicht ausgefuehrt.
Der neue normale Kernel-/Modulbau und der kurze SMP4-Kernel-Smoke bestehen,
einschliesslich Clock-, TLB-, Lock-, Serial- und NVMe-IRQ-Abnahme.
Runtimegast: 21,02 s; passiver Fallbackgast: 20,32 s, beide SMP4/KVM mit
frischen 2-GB-Images, 1 GB RAM, ohne Gastnetz oder NVIDIA-Geraet. Das sind
Ablaufzeiten und keine Grafikbenchmarks. Der gesamte bisherige Shutdown
bleibt erfolgreich: generisch drei Tasks, zwei Semaphoren, zwei CPU-Puffer
mit 4185 Bytes, bootfb/DISPLAYD und regulaeres Ausschalten.

Inventare: 713 Testeintraege, 147 Module, 108 Dokumente. Drei Quellen sind
in vorhandene Testeinstiege aufgenommen; kein neues Modul oder Workspace-
Gate. Fruehe Compiler-/Packagerfehler sind separat erhalten und zaehlen
nicht als Erfolg. Belege: native_abort_checkpoint sowie
ExFiles/Reference/GFX/Nvidia/0.79.10/native-abort. Alle frueheren Checkpoints
bleiben als historische Nachweise semantisch unveraendert.

Der getrennte volle RM-/NVKMS-Partiallink besitzt weiterhin offene private
Anbieter. Sein kompletter Ziel-Link, weitere OS-/Kryptofunktionen, GSP/RPC,
GPU-Authentifizierung/Initialisierung, native Anzeige und HDMI-/IRQ-Hardware
bleiben offen. Diese CPU-Abnahme ersetzt keinen dieser Schritte.

Native Wartefunktionen und monotone Aufruffristen (10.09.2026)
-----------------------------------------------------------
NVIDIA 0.1.11 linkt os_delay_us, os_delay, os_schedule, nvkms_usleep und
nvkms_yield ueber die beiden echten privaten Anbieter r4nv_wait_ns und
r4nv_schedule. Insgesamt 42 original-headerbasierte C-Adapter und zwoelf
private Anbieter im normalen R4D; keine offenen ELF-Symbole, TLS oder
automatischen Initialisierer. Keine neue vendor-Implementierung im Ziel;
dieselben 19 Originalheader mit vollstaendigen Notices bleiben bytegleich.

Busy-Waits pruefen die wirkliche monotone Zeit. Laengere schlaffaehige
Pausen uebergeben ganze Ticks an den Scheduler und pruefen nach jedem
Wakeup die Uhr erneut; erst der Rest wird aktiv abgewartet. Fruehes
Aufwachen ist kein Erfolg. RM-Mikrosekunden bleiben busy, NVKMS-Pausen
unter 1000us ebenso; groessere NVKMS-Pausen verlangen schlaffaehigen Kontext.
Die volle 64-Bit-Zeit bleibt erhalten, Multiplikations-/Endzeitueberlauf
wird abgewiesen. Die zwoelf Bit breite Millisekundenmaske des gepinnten
Linux-Helfers wird nicht kopiert. Im Gast werden mindestens 4100ms an
einer echten NVKMS-Pause geprueft.

IRQ-Busy-Requests ueber 20ms werden gemaess der gepinnten Primaerreferenz
abgelehnt. Ein rein periodischer Ereignistakt darf bei abgeschalteten
Interrupts keinen Busy-Wait versorgen. Ungueltige, ruecklaeufige oder
feststehende Uhren liefern Fehler und schliessen weitere normale native
Zulassung. Die Grenze unveraenderter Ablesungen erkennt fehlenden
Fortschritt, sie ist keine kalibrierte Zeitangabe. Die primaeren Originale
nv-time.h, Linux-os-interface.c, nvidia-modeset-linux.c und NVKMS-OS-Header
samt COPYING und Hash-/Revisionsindex liegen im neuen Nachweisarchiv.

Jede normale caller-eigene Invocation verlangt eine absolute monotone
Frist. Vor Start, bei Eintritt, in Wait/Yield und nach normaler Rueckkehr
wird diese geprueft. Ein bereits vorhandener Callbackfehler bleibt erhalten.
Die aktuelle Invocation stammt ueber current_request aus dem tatsaechlich
laufenden Task, ohne globale Zuordnungstabelle oder CPU-lokale Vermutung.
Der unveraenderte caller-eigene Datensatz bleibt bis zur Taskfreigabe
resident. Explizites Cleanup darf ohne Frist zugelassen werden; damit ist
begrenzter Ressourcenabbau auch nach Uhrverlust weiterhin moeglich.
Ein Deadlinefehler ist -76002, Uhrverlust -76003. Dies beendet keine
beliebige upstream Endlosschleife und entzieht keinen GPU-/DMA-Zugriff.

Kernel 0.1.146 erweitert ausschliesslich die optionale ThreadApi um
current_request bei Offset80 (Version1, jetzt88 Bytes). Kapazitaeten72..79,
80..87 und ab88 erhalten den jeweils vollstaendigen Praefix72/80/88;
Randbytes bleiben unveraendert. Ungueltige Requestpraefixe bleiben intakt,
ein gueltiger wird vor einer Kontext-/Besitzerablehnung geleert. Der neue
Statuswert8 wird aus dem wirklich blockierten Task in seiner Sleepqueue
abgeleitet; Statusflags4/8 bleiben als Startflags ungueltig. DriverApi33/632,
Importnamen und RELEASE0.79.8 bleiben erhalten. Keine neue Allokation,
Warteoperation oder weiterer Unwind-Guard in der Taskbindungsabfrage.

Stop weckt den tatsaechlich schlafenden dedizierten Task; RM erhaelt
NV_ERR_SIGNAL_PENDING und keine angeblich erfolgreich abgelaufene Pause.
Fristablauf bleibt NV_ERR_TIMEOUT; void-NVKMS-Fehler nutzen den vorhandenen
nativen Selbstabbruch. Init/Work koennen ihren schlaffaehigen Legacy-Tickpfad
nutzen, nach dessen Rueckkehr die echte Zeit geprueft wird. Echter Yield
verlangt einen dedizierten Task: waitTicks(0) im alten ABI ist ein No-op
und wird nicht als Scheduleruebergabe ausgegeben.

Die vorhandene SMP4-Probe bestaetigt vor Stop die reale Sleepqueue samt
blockierter Task, prueft parallele 250ms-/5s-Aufruffristen, abgelaufene
Neuzulassung ohne Callbackeintritt und vollstaendige Taskfreigabe. Alle
bisherigen nativen C-/Semaphore-/Abbruch-/Cleanup-Proben bleiben erfolgreich,
einschliesslich behaltenen 145 Bytes bei Busy-Free und spaeterem echtem
Permit. Der generische Shutdown hat weiterhin drei Tasks, zwei Semaphoren
und zwei CPU-Puffer mit 4185 Bytes; bootfb/DISPLAYD und Poweroff funktionieren.
Runtimegast 26.86s, passiver Fallback 20.33s;
frische SMP4/KVM-Images ohne NVIDIA-Geraet oder Gastnetz. Keine GPU-Messung.

421 Kerneltests, 13 Contractschritte, zehn Zig-Fassadenfaelle plus C,
21 NVIDIA-Besitzerfaelle einschliesslich vier gezielter Zeitpolicyfaelle,
normaler Kernel-/Modulbau und kurzer SMP4-Kernel-Smoke bestehen. Die
bestehende C-Fassade kompiliert auch fuer Windows; dort keine Ausfuehrung.
Die getrennte C-Adapterabnahme prueft 58 Bedingungen/13 noreturn-Fehler auf
den exakten freestanding Objekten. Kein neuer Workspace-Testeinstieg.

Zwei frische volle Quellbuilds ueber 1196 originale Uebersetzungseinheiten
und 3156 verifizierte Dateien liefern bytegleiche Partiallinks. Alle acht
Shaderfamilien bleiben exakt geprueft; acht Adapterobjekte/17 versionierte
Inputs werden integriert. RM hat 328 offene Symbole, NVKMS 48, darunter die
privaten Zielanbieter. Weitere OS-/Kryptofunktionen und der volle R4D-Link
bleiben offen; der Partiallink ist kein installierter GPU-Treiber.
RM-Partiallink: 14664968 Bytes,
SHA256 7d8e70315b275ea6a4c813677181c5d1fb2736b6d38964285744db3b5ec05aaa.
NVKMS-Partiallink: 2942456 Bytes,
SHA256 34f603525df92edb8ef681650dcfb0987eeac8de9904a2b206a8ebfe0ebf79de.

R4D-Datei 92293808 Bytes, allokiert 81920 Bytes
(80 KB), 1563 Relocations
(83 ABS64/1480 REL32), ein Import/zwei Exports.
Fuenf nicht allokierte Ressourcen, echte NTFS-Treiber-/Lizenzdateien beider
Images und danebenliegende Legal-Dateien stimmen mit den kanonischen
Bytes ueberein. Strenge Relocation-/Lizenzpruefungen bleiben erhalten.

717 Testquellen, 147 Module, 108 Dokumente. Vier Quellen ergaenzen bestehende
Abnahmeeinstiege, kein neues Modul oder Workspace-Gate. Nachweise:
native_waits_checkpoint und ExFiles/Reference/GFX/Nvidia/0.79.10/native-waits.
Alle frueheren JSON-Checkpoints bleiben semantisch unveraendert. OssiPC
wurde nicht kontaktiert. GSP/RPC, GPU-Authentifizierung, nativer Display-
und HDMI-Pfad sowie Hardwareabnahmen bleiben offen. Auf Useranweisung
wird nur noch an 0.79.10 gearbeitet und danach pausiert; kein Uebergang
zur naechsten Unterversion und kein unbelegter Abschluss.

Native Formatierung und Diagnoseausgabe (NVIDIA 0.1.15, 10.09.2026)
----------------------------------------------------------------
Neun weitere unveraenderte Originaldeklarationen sind im R4D angebunden:
os_snprintf/os_vsnprintf, nvkms_snprintf/nvkms_vsnprintf, nv_printf,
out_string, os_log_error, os_dbg_set_level und nvkms_log. Zusammen sind es
51 C-Adapter und 13 private Anbieter; Kernel/API bleiben unveraendert.

Der private ganzzahlige Formatter versteht d/i/u/o/x/X, c/s/p/%, Flags,
Feldbreite/Praezision einschliesslich Sternargumenten und hh/h/l/ll/j/z/t.
snprintf liefert die erforderliche Laenge auch bei kleinerem Ziel oder
Groesse null. Reale Writes und gemeinsame Eingabescans sind auf 64 KB
begrenzt; riesige wegfallende Auffuellbereiche werden nur gezaehlt. Ueberlauf,
unbekannte Formate, Gleitkomma, breite Zeichen, Positionsargumente, %n und
Linux-Pointererweiterungen liefern -1 und eine leere Ausgabe. %p zeigt die
rohe Adresse mit sechzehn Hexstellen ohne automatisch vorangestelltes 0x;
vorhandene NVIDIA-Formate wie 0x%p bleiben damit eindeutig.

Jeder Logaufruf benutzt einen eigenen begrenzten Puffer. Die vorhandene
DriverApi ordnet den einzelnen Datensatz dem realen Treiberbesitzer zu;
kein globaler Textpuffer, neue Heapallokation oder neuer Kernellogpfad.
Schweregrad und NVIDIAs Schwellwertkodierung bleiben erhalten. Lange Logs
tragen einen sichtbaren Abbruchhinweis; nv_printf meldet tatsaechlich
uebergebene Bytes, snprintf weiterhin die erforderliche Gesamtlange.
Formatfehler erzeugen eine ausdrueckliche Fehlermeldung. Nach Unbind
verweigert der private Loganbieter neue Ausgaben.

Der bestehende unit-test enthaelt 24 Zig-Faelle und denselben C-Formattest
mit 6588 Pruefungen, darunter 5833 Vergleiche gegen die Host-libc.
Geschuetzte Seitengrenzen pruefen kurze Ziele und praezisionsbegrenzte
Strings ohne lesbaren Nullterminator. Dieselben vier freestanding Objekte
laufen nochmals im vorhandenen build-rm; dort bleiben Zielanbieter offen.
Die Windows-Fassung ist kompiliert, aber nicht auf Windows ausgefuehrt.

SMP4/KVM: Runtime 27.57s und passiver Fallback 20.22s, frische Images ohne
Gastnetz oder NVIDIA-Geraet. Alle neun C-Pfade laufen in Init, Driver Work
und einem dedizierten nativen Task. Der Gastnachweis prueft zusammenhaengende
Logbytes, 64-Bit-Wert, Schweregrad und identische nichtnull Treiber-ID.
Bisherige Wait-/Semaphore-/Abbruch-/Cleanup-Nachweise bleiben erfolgreich.
Keine neue Gatefamilie, kein GPU-, Scanout- oder HDMI-Nachweis.

Der volle Quellbau ueber 1196 originale Uebersetzungseinheiten und 3156
verifizierte Dateien nimmt nun zwoelf Adapterobjekte auf. RM hat 325,
NVKMS 46 offene Symbole; beide Partiallinks sind weiterhin unvollstaendig.
Das R4D selbst hat keine offenen Symbole, TLS oder Initialisierer:
92335248 Dateibytes, 114688 allokierte Bytes (112 KB), 1909 Relocations,
ein Import und zwei Exports. Alle fuenf bisherigen Ressourcen bleiben
bytegleich; kein fremder Implementierungscode oder Lizenzwechsel.

Nachweise: native_logging_checkpoint und
ExFiles/Reference/GFX/Nvidia/0.79.10/native-logging. Release bleibt 0.79.9;
0.79.10, der volle native RM-/NVKMS-Link, FWSEC/GSP/RPC, Thermik, native
Anzeige, HDMI und ihre Hardwareabnahmen bleiben offen.
